import{_ as t,o as a,a as s,a2 as i}from"./chunks/framework.DCcmcJcL.js";const u=JSON.parse('{"title":"Best Security Practices","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/best-security-practice.md","filePath":"getting-started/best-security-practice.md","lastUpdated":1790143103000}'),o={name:"getting-started/best-security-practice.md"};function n(r,e,d,c,l,h){return a(),s("div",null,e[0]||(e[0]=[i(`<h1 id="best-security-practices" tabindex="-1">Best Security Practices <a class="header-anchor" href="#best-security-practices" aria-label="Permalink to &quot;Best Security Practices&quot;">​</a></h1><p>Bagisto has several protections switched on by default, and a few that you switch on or configure yourself. This page lists both, then the server settings that matter for a Bagisto store; work through it before a store goes live.</p><h2 id="what-bagisto-already-does" tabindex="-1">What Bagisto Already Does <a class="header-anchor" href="#what-bagisto-already-does" aria-label="Permalink to &quot;What Bagisto Already Does&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Protection</th><th>What it does</th><th>Where</th></tr></thead><tbody><tr><td>Security headers</td><td>Adds <code>Strict-Transport-Security</code>, <code>X-Frame-Options</code>, <code>X-Content-Type-Options</code>, <code>X-XSS-Protection</code> and <code>Referrer-Policy</code> to every response</td><td><code>Webkul\\Core\\Http\\Middleware\\SecureHeaders</code>, appended in <code>bootstrap/app.php</code></td></tr><tr><td>CSRF protection</td><td>Checks the CSRF token on every <code>web</code> route, except <code>stripe/*</code>, the PayU, PhonePe and PayGlocal callback and webhook routes, and the web installer&#39;s <code>install/api/*</code> routes</td><td><code>bootstrap/app.php</code> for <code>stripe/*</code>; the others call <code>withoutMiddleware(PreventRequestForgery::class)</code> in their package&#39;s <code>Routes/web.php</code></td></tr><tr><td>Admin permissions</td><td>Refuses an admin route that isn&#39;t mapped in the ACL, for every role without full access</td><td><code>Webkul\\User\\Http\\Middleware\\Bouncer</code></td></tr><tr><td>Two-factor authentication</td><td>Lets each admin protect their account with an authenticator app</td><td><code>Webkul\\User\\TwoFactorAuthentication</code></td></tr><tr><td>HTML purification</td><td><code>clean_content()</code> runs HTML through HTMLPurifier with the allowlist in <code>config/purify.php</code>, then strips Blade syntax and PHP tags</td><td><code>packages/Webkul/Core/src/Http/helpers.php</code></td></tr><tr><td>SVG sanitizing</td><td>Removes scripts and remote references from SVG files uploaded through the editor, review attachments, image search and locale logos</td><td><code>Webkul\\Core\\Traits\\Sanitizer</code></td></tr><tr><td>Installer lock</td><td>Refuses the web installer once the store is installed</td><td><code>Webkul\\Installer\\Http\\Middleware\\CanInstall</code></td></tr><tr><td>Rate limits</td><td>Allows five two-factor verification or setup attempts a minute, and six resends of the customer verification email a minute</td><td><code>packages/Webkul/Admin/src/Routes/</code>, <code>packages/Webkul/Shop/src/Routes/customer-routes.php</code></td></tr></tbody></table><p><code>SecureHeaders</code> sends <code>X-Frame-Options: DENY</code>, except on a response a controller marks as framable, such as the storefront preview in the Appearance editor, which gets <code>Content-Security-Policy: frame-ancestors &#39;self&#39;</code> instead. It also sends <code>Strict-Transport-Security: max-age=31536000; includeSubDomains</code>. Once a browser has seen that header over HTTPS, it refuses plain HTTP for the domain and all of its subdomains for a year, so serve every subdomain over HTTPS before going live.</p><h2 id="change-the-admin-url" tabindex="-1">Change the Admin URL <a class="header-anchor" href="#change-the-admin-url" aria-label="Permalink to &quot;Change the Admin URL&quot;">​</a></h2><p>Every admin route is prefixed with <code>config(&#39;app.admin_url&#39;)</code>, which reads <code>APP_ADMIN_URL</code> and defaults to <code>admin</code>:</p><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">APP_ADMIN_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=backoffice</span></span></code></pre></div><p>After changing it, clear the cached configuration and routes with <code>php artisan optimize:clear</code>, and in production run <code>php artisan optimize</code> again. The admin sign-in page is then at <code>/backoffice/login</code>.</p><p>A different prefix keeps automated scans away from the sign-in page. It doesn&#39;t replace two-factor authentication or an IP allowlist.</p><h2 id="use-two-factor-authentication-for-admins" tabindex="-1">Use Two-Factor Authentication for Admins <a class="header-anchor" href="#use-two-factor-authentication-for-admins" aria-label="Permalink to &quot;Use Two-Factor Authentication for Admins&quot;">​</a></h2><p>Each admin turns on two-factor authentication for their own account, from the account page of the admin panel. Bagisto shows a QR code for a time-based authenticator app (through <code>pragmarx/google2fa</code>) and eight single-use backup codes. From then on, after that admin signs in, the <code>Bouncer</code> middleware opens no admin page until they enter a code from the app or a backup code.</p><ul><li>No setting forces every admin to use it. Make it a rule for accounts with full permissions.</li><li>The secret is stored encrypted with <code>APP_KEY</code>. If <code>APP_KEY</code> changes, the saved secrets can&#39;t be decrypted, and those admins can&#39;t pass verification.</li><li>Backup codes are stored as hashes. An admin who loses both the device and the codes needs someone with database access to clear the <code>two_factor_*</code> columns of their row in the <code>admins</code> table, which is what turning the feature off does (<code>TwoFactorAuthentication::getDisableValues()</code>).</li></ul><h2 id="production-environment-settings" tabindex="-1">Production Environment Settings <a class="header-anchor" href="#production-environment-settings" aria-label="Permalink to &quot;Production Environment Settings&quot;">​</a></h2><p>Set the keys in step 1 of the <a href="./deployment.html#production-checklist">production checklist</a>. Three matter most for security:</p><ul><li><strong><code>APP_DEBUG=false</code>.</strong> Otherwise error pages show code, queries and environment values.</li><li><strong><code>SESSION_SECURE_COOKIE=true</code></strong>, with an <code>https://</code> <code>APP_URL</code>, so the session cookie is only sent over HTTPS.</li><li><strong><code>APP_KEY</code></strong>, generated once and kept secret. It encrypts cookies, sessions and two-factor secrets.</li></ul><p><code>config/session.php</code> serializes sessions with <code>php</code>. Setting <code>SESSION_SERIALIZATION=json</code> protects against object injection if <code>APP_KEY</code> ever leaks, but switching logs out every existing session.</p><h2 id="code-you-add" tabindex="-1">Code You Add <a class="header-anchor" href="#code-you-add" aria-label="Permalink to &quot;Code You Add&quot;">​</a></h2><p>The protections above only cover your own code when it uses them:</p><ul><li>Pass stored HTML through <code>clean_content()</code> before you print it unescaped.</li><li>Give every admin route of your package an entry in its <code>acl.php</code>; see <a href="./../package-development/access-control-list.html">Access Control List</a>.</li><li>Validate every upload with an explicit <code>mimes:</code> list, and don&#39;t accept <code>svg</code> in an upload that customers can reach.</li><li>Don&#39;t exclude a route from CSRF checks, in <code>bootstrap/app.php</code> or with <code>withoutMiddleware()</code>, to make a form work. A gateway callback that skips the check must verify the gateway&#39;s signature instead.</li></ul><h2 id="web-server" tabindex="-1">Web Server <a class="header-anchor" href="#web-server" aria-label="Permalink to &quot;Web Server&quot;">​</a></h2><ul><li><p><strong>Serve only <code>public/</code>.</strong> Point the document root at <code>public/</code>, never at the project root, where <code>.env</code>, <code>storage/</code> and <code>vendor/</code> would become downloadable. See <a href="./deployment.html#web-server-configuration">Web Server Configuration</a>.</p></li><li><p><strong>Deny dotfiles.</strong> The production Docker images block every path segment that starts with a dot, except <code>.well-known</code>:</p><p><strong>File:</strong> <code>docker/production/nginx/config/nginx.conf</code></p><div class="language-nginx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">nginx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">location</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ~</span><span style="--shiki-light:#032F62;--shiki-dark:#DBEDFF;"> /\\.(?!well-known).* </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    deny </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">all</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></li><li><p><strong>Turn off directory listings.</strong> <code>public/.htaccess</code> sets <code>Options -MultiViews -Indexes</code> for Apache, and the production Apache virtual host sets <code>Options -Indexes +FollowSymLinks</code>.</p></li><li><p><strong>Don&#39;t run PHP from uploads.</strong> Uploaded files are stored in <code>storage/app/public</code> and served through the <code>public/storage</code> link. The Nginx configuration on the deployment page passes only <code>index.php</code> to PHP-FPM. On Apache, add this to the virtual host:</p><div class="language-apache vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">apache</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">LocationMatch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;^/storage/.+\\.php$&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    Require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> all denied</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">LocationMatch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div></li><li><p><strong>Restrict the admin panel by IP address.</strong> With Nginx, add a location for your admin prefix above the other <code>location</code> blocks, and replace the example address with yours:</p><div class="language-nginx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">nginx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">location</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ~</span><span style="--shiki-light:#032F62;--shiki-dark:#DBEDFF;"> ^/admin(/|$) </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    allow </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">203.0.113.10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    deny </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">all</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try_files </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$uri $uri/ /index.php?$query_string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></li><li><p><strong>Hide platform details.</strong> The production image&#39;s <code>php.ini</code> sets <code>expose_php = Off</code> and <code>display_errors = Off</code>. <code>SecureHeaders</code> also sends <code>X-Built-With: Bagisto</code>; to drop it, add <code>fastcgi_hide_header X-Built-With;</code> to the Nginx PHP location.</p></li></ul><h2 id="report-a-vulnerability" tabindex="-1">Report a Vulnerability <a class="header-anchor" href="#report-a-vulnerability" aria-label="Permalink to &quot;Report a Vulnerability&quot;">​</a></h2><p>Don&#39;t open a public issue. Email <strong><a href="mailto:support@bagisto.com" target="_blank" rel="noreferrer">support@bagisto.com</a></strong>, or open a ticket at <a href="https://bagisto.uvdesk.com/en/" target="_blank" rel="noreferrer">Bagisto Support</a>, as <code>SECURITY.md</code> in the Bagisto repository describes.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./deployment.html">Deployment</a>: the rest of a production setup, including web server configurations.</li><li><a href="./../package-development/access-control-list.html">Access Control List</a>: permissions for your package&#39;s admin routes.</li></ul>`,26)]))}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
