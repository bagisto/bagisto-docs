import{_ as a,o as t,a as s,a2 as o}from"./chunks/framework.DCcmcJcL.js";const g=JSON.parse('{"title":"Cache Strategy","description":"","frontmatter":{},"headers":[],"relativePath":"advanced/cache-strategy.md","filePath":"advanced/cache-strategy.md","lastUpdated":1789464754000}'),i={name:"advanced/cache-strategy.md"};function r(d,e,c,n,h,l){return t(),s("div",null,e[0]||(e[0]=[o(`<h1 id="cache-strategy" tabindex="-1">Cache Strategy <a class="header-anchor" href="#cache-strategy" aria-label="Permalink to &quot;Cache Strategy&quot;">​</a></h1><p>Bagisto caches at several layers, each with its own store and its own way of being invalidated. This page explains what each layer holds and what clears it, so a package can take part without serving stale data. Setting the caches up for production is on <a href="./../performance/configure-fpc.html">Configure Full Page Cache</a> and <a href="./../performance/configure-load-balancing.html">Configure Load Balancing</a>.</p><h2 id="cache-layers" tabindex="-1">Cache Layers <a class="header-anchor" href="#cache-layers" aria-label="Permalink to &quot;Cache Layers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>What it holds</th><th>Stored in</th><th>Cleared by</th></tr></thead><tbody><tr><td>Application cache</td><td>Laravel&#39;s cache, and the two layers below it</td><td><code>CACHE_STORE</code></td><td>Writes, events, <code>cache:clear</code></td></tr><tr><td>Repository cache</td><td>Reads of six core repositories</td><td>The application cache</td><td>Writes through the repository</td></tr><tr><td>Catalog API cache</td><td>Guest responses of the storefront product and category APIs</td><td>The application cache</td><td>Catalog, price, review and order events</td></tr><tr><td>Full page cache</td><td>Guest storefront HTML</td><td><code>RESPONSE_CACHE_DRIVER</code></td><td>The <code>Webkul\\FPC</code> listeners, <code>responsecache:clear</code></td></tr><tr><td>Image cache</td><td>Nothing on the server; resized images carry HTTP cache headers</td><td>Browsers and any CDN in front</td><td>Expiry</td></tr><tr><td>Framework caches</td><td>Configuration, routes, events and compiled views</td><td><code>bootstrap/cache</code>, <code>storage/framework/views</code></td><td><code>optimize:clear</code></td></tr></tbody></table><h2 id="application-cache" tabindex="-1">Application Cache <a class="header-anchor" href="#application-cache" aria-label="Permalink to &quot;Application Cache&quot;">​</a></h2><p><code>config/cache.php</code> reads the store from <code>CACHE_STORE</code>. On Bagisto 2.5 the default, in <code>config/cache.php</code> and in <code>.env.example</code>, is <code>database</code>, backed by the <code>cache</code> and <code>cache_locks</code> tables of the root migration <code>2026_02_03_151925_create_cache_table.php</code>; on Bagisto 2.4 it was <code>file</code>.</p><p><code>config/cache.php</code> sets <code>serializable_classes</code> to <code>true</code> on purpose. The repository cache stores Eloquent models and collections, which Laravel 13&#39;s default of <code>false</code> would return as incomplete objects.</p><p>For Redis, set <code>CACHE_STORE=redis</code>. <code>.env.example</code> sets <code>REDIS_CLIENT=phpredis</code>, which needs the <code>phpredis</code> PHP extension; <code>predis/predis</code> is installed with Bagisto, so <code>REDIS_CLIENT=predis</code> works without it.</p><h2 id="repository-cache" tabindex="-1">Repository Cache <a class="header-anchor" href="#repository-cache" aria-label="Permalink to &quot;Repository Cache&quot;">​</a></h2><p>Every Bagisto repository extends <code>Webkul\\Core\\Eloquent\\Repository</code>, which implements the Prettus <code>CacheableInterface</code>. Caching is off by default (<code>&#39;enabled&#39; =&gt; false</code> in <code>config/repository.php</code>) and switched on for the repositories under <code>cache.repositories</code>: <code>CoreConfigRepository</code>, <code>ChannelRepository</code>, <code>CountryRepository</code>, <code>CountryStateRepository</code>, <code>CurrencyRepository</code> and <code>LocaleRepository</code>, the ones read on nearly every request.</p><ul><li><strong>Cached methods.</strong> The read methods of the Prettus <code>CacheableRepository</code> trait: <code>all()</code>, <code>paginate()</code>, <code>findByField()</code>, <code>findWhere()</code> and <code>getByCriteria()</code>, plus Bagisto&#39;s <code>findOneByField()</code> and <code>findOneWhere()</code>, which call them. <code>Repository</code> redefines <code>find()</code> and <code>findOrFail()</code> without caching, and methods the trait doesn&#39;t define, such as <code>findWhereIn()</code>, <code>first()</code> and <code>count()</code>, always query the database.</li><li><strong>Lifetime.</strong> Reads are kept for the global <code>cache.minutes</code> (10080), unless the repository class sets a <code>$cacheMinutes</code> property. A <code>minutes</code> key inside a <code>cache.repositories</code> entry isn&#39;t read.</li><li><strong>Key.</strong> It&#39;s built from the method, its arguments, the criteria and the full request URL, so the same read made on two URLs is cached twice.</li><li><strong>Bypass.</strong> A request carrying the <code>skipCache</code> parameter (<code>cache.params.skipCache</code>) reads past the cache.</li></ul><h3 id="invalidation" tabindex="-1">Invalidation <a class="header-anchor" href="#invalidation" aria-label="Permalink to &quot;Invalidation&quot;">​</a></h3><p>Each cached repository has a generation token, stored in the application cache under <code>repository-generation:{class}</code> and mixed into every key. <code>Webkul\\Core\\Listeners\\CleanCacheRepository</code> listens for the Prettus <code>RepositoryEntityCreated</code>, <code>RepositoryEntityUpdated</code> and <code>RepositoryEntityDeleted</code> events, which the repository&#39;s <code>create()</code>, <code>update()</code> and <code>delete()</code> fire, and calls <code>Webkul\\Core\\Helpers\\CacheGeneration::bump()</code>. The old entries become unreachable and expire on their own. The listener honours a per-repository <code>clean.on.created</code>, <code>updated</code> or <code>deleted</code> entry; switching cleaning off altogether is only the global <code>cache.clean.enabled</code>.</p><p>A write that bypasses the repository, such as a model&#39;s <code>save()</code> or a <code>DB::table()</code> update, fires none of these events and leaves the cached reads in place.</p><p><code>CacheGeneration</code> also keeps the tokens it has read in a static property, <code>CacheGeneration::$tokens</code>, which nothing clears outside the test suite. A PHP-FPM request starts with it empty, but a queue worker or an Octane worker keeps the first token it read for each repository across jobs and requests, so its cached reads keep returning old values after another process writes. A listener that calls <code>CacheGeneration::flush()</code> fixes it; see <a href="./queue-jobs-scheduling.html#refreshing-repository-cache-tokens-between-jobs">Queues, Jobs and Scheduling</a> and <a href="./../performance/configure-laravel-octane.html#repository-cache-tokens">Configure Laravel Octane</a>.</p><h3 id="caching-your-own-repository" tabindex="-1">Caching Your Own Repository <a class="header-anchor" href="#caching-your-own-repository" aria-label="Permalink to &quot;Caching Your Own Repository&quot;">​</a></h3><p>Turn caching on for a repository by adding it to <code>repository.cache.repositories</code> from your package&#39;s service provider, so <code>config/repository.php</code> stays untouched:</p><p><strong>File:</strong> <code>packages/Webkul/Blog/src/Providers/BlogServiceProvider.php</code></p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">php</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Webkul\\Blog\\Providers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Illuminate\\Support\\ServiceProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\Blog\\Repositories\\PostRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BlogServiceProvider</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ServiceProvider</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * Register services.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &#39;repository.cache.repositories.&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PostRepository</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;.enabled&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Set the lifetime with a <code>protected $cacheMinutes = 60;</code> property on <code>PostRepository</code>, documented like any other property. An entry in <code>cache.repositories</code> can also set <code>allowed.only</code>, to cache only some methods, and <code>clean.on.created</code>, <code>updated</code> or <code>deleted</code>. Use <code>allowed.only</code> rather than <code>allowed.except</code>, which the per-repository lookup doesn&#39;t read. Cache a repository only when every write to its table goes through it.</p><h2 id="catalog-api-cache" tabindex="-1">Catalog API Cache <a class="header-anchor" href="#catalog-api-cache" aria-label="Permalink to &quot;Catalog API Cache&quot;">​</a></h2><p>The storefront product listing and category APIs cache their responses for guests through <code>Webkul\\Shop\\Helpers\\CatalogApiCache</code>. A response is kept for an hour under a key built from a catalog version number, the channel, its theme, the locale, the currency and the request parameters. Signed-in customers aren&#39;t cached, because their responses carry wishlist state and group prices, and product searches with a term aren&#39;t cached either.</p><p>Guest responses are sent with <code>Cache-Control: max-age=60, public</code> and <code>Vary: Cookie</code>; customer responses with <code>Cache-Control: no-cache, private</code>.</p><p><code>Webkul\\Shop\\Listeners\\CatalogCache::flush()</code> increments the version (<code>shop_api_catalog_version</code>), which moves every cached response onto new keys. The Shop <code>EventServiceProvider</code> calls it on product and category create, update and delete, on <code>catalog.product.price.reindex.after</code> and <code>promotions.catalog_rule.reindex.after</code>, on review update and delete, and on <code>checkout.order.save.after</code>, <code>sales.order.cancel.after</code> and <code>sales.refund.save.after</code>. Imports fire none of these. To invalidate the API cache from a package, call <code>app(CatalogApiCache::class)-&gt;flush()</code> from a listener on your own event.</p><h2 id="full-page-cache" tabindex="-1">Full Page Cache <a class="header-anchor" href="#full-page-cache" aria-label="Permalink to &quot;Full Page Cache&quot;">​</a></h2><p><code>Webkul\\FPC</code> stores guest storefront pages through <a href="https://github.com/spatie/laravel-responsecache" target="_blank" rel="noreferrer"><code>spatie/laravel-responsecache</code></a> 8. The profile, hasher and replacers are wired in <code>config/responsecache.php</code>; the middleware alias is set in <code>ShopServiceProvider::boot()</code>:</p><table tabindex="0"><thead><tr><th>Piece</th><th>Class</th><th>Job</th></tr></thead><tbody><tr><td>Middleware</td><td><code>Webkul\\Shop\\Http\\Middleware\\CacheResponse</code>, aliased as <code>cache.response</code></td><td>Follows search-term and URL-rewrite redirects, then serves or stores the page</td></tr><tr><td>Cache profile</td><td><code>Webkul\\FPC\\CacheProfiles\\FullPageCacheProfile</code></td><td>Decides whether a request is cached and for how long</td></tr><tr><td>Hasher</td><td><code>Webkul\\FPC\\Hasher\\DefaultHasher</code></td><td>Builds the cache key</td></tr><tr><td>Replacers</td><td><code>CsrfTokenReplacer</code>, <code>Webkul\\FPC\\Replacers\\FlashMessagesReplacer</code>, <code>MiniCartReplacer</code></td><td>Put visitor-specific values into a cached page on every hit</td></tr></tbody></table><p>A page is cached when its route carries <code>cache.response</code>, the request is a non-AJAX <code>GET</code> whose response is successful or a redirect with a text or JSON content type, the <code>cache_management.full_page_cache.settings.enabled</code> setting is on (the default), and no customer is signed in. These Shop routes carry the middleware:</p><table tabindex="0"><thead><tr><th>Route</th><th>Page</th></tr></thead><tbody><tr><td><code>shop.home.index</code></td><td>The home page</td></tr><tr><td><code>shop.product_or_category.index</code></td><td>Every product and category page (the fallback route)</td></tr><tr><td><code>shop.cms.page</code></td><td>CMS pages</td></tr><tr><td><code>shop.home.contact_us</code></td><td>The contact page</td></tr><tr><td><code>shop.home.contact_us.send_mail</code></td><td>Carries the middleware, but as a <code>POST</code> it&#39;s never stored</td></tr><tr><td><code>shop.search.index</code></td><td>The search results page</td></tr><tr><td><code>shop.compare.index</code></td><td>The compare page</td></tr></tbody></table><p>The key is built from the host, the path, the HTTP method and a suffix of <code>{channel}-{locale}-{currency}-</code>, so each page is stored once per host, channel, locale and currency. The hasher drops the query string, except <code>query</code> on the search page. Customer account routes and every admin route carry <code>Webkul\\Core\\Http\\Middleware\\NoCacheMiddleware</code>, which sends <code>Cache-Control: no-store, no-cache, must-revalidate, max-age=0</code> to browsers and proxies.</p><p>A view that prints something specific to the visitor inside a cached route must not write it into the page. The flash messages and mini cart views call <code>Webkul\\FPC\\FullPageCache::willCache()</code> and, when it returns <code>true</code>, print a marker that their replacer swaps for the live value on every response.</p><h3 id="what-invalidates-the-cache" tabindex="-1">What Invalidates the Cache <a class="header-anchor" href="#what-invalidates-the-cache" aria-label="Permalink to &quot;What Invalidates the Cache&quot;">​</a></h3><p>The listeners are in <code>packages/Webkul/FPC/src/Listeners/</code> and registered in <code>Webkul\\FPC\\Providers\\EventServiceProvider</code>:</p><table tabindex="0"><thead><tr><th>Event</th><th>Listener</th><th>Forgets</th></tr></thead><tbody><tr><td><code>catalog.product.create.after</code>, <code>catalog.product.update.after</code>, <code>catalog.product.delete.before</code></td><td><code>Product</code></td><td>The home page, and the page and category pages of the product and its related products</td></tr><tr><td><code>catalog.category.create.after</code></td><td><code>Category</code></td><td>The home page</td></tr><tr><td><code>catalog.category.update.after</code>, <code>catalog.category.delete.before</code></td><td><code>Category</code></td><td>The home page and the category&#39;s page in every locale</td></tr><tr><td><code>catalog.product.price.reindex.after</code>, <code>promotions.catalog_rule.reindex.after</code></td><td><code>Price</code></td><td>The affected products&#39; pages as above, or the whole cache when no product ids are passed (a full price reindex)</td></tr><tr><td><code>customer.review.update.after</code>, <code>customer.review.delete.before</code></td><td><code>Review</code></td><td>The product&#39;s page</td></tr><tr><td><code>checkout.order.save.after</code>, <code>sales.order.cancel.after</code></td><td><code>Order</code></td><td>The ordered products&#39; pages as above</td></tr><tr><td><code>sales.refund.save.after</code></td><td><code>Refund</code></td><td>The refunded products&#39; pages as above</td></tr><tr><td><code>cms.page.update.after</code>, <code>cms.page.delete.before</code></td><td><code>Page</code></td><td><code>/page/{url_key}</code> for every translation</td></tr><tr><td><code>marketing.search_seo.url_rewrites.update.after</code>, <code>marketing.search_seo.url_rewrites.delete.before</code></td><td><code>URLRewrite</code></td><td>The rewrite&#39;s request path</td></tr><tr><td><code>section.create.after</code>, <code>section.update.after</code>, <code>section.delete.before</code></td><td><code>Section</code></td><td>The whole cache when the section type renders in the layout, otherwise the home page</td></tr><tr><td><code>core.channel.update.after</code></td><td><code>Channel</code></td><td>The whole cache</td></tr><tr><td><code>core.configuration.save.after</code></td><td><code>CoreConfig</code></td><td>The whole cache</td></tr></tbody></table><p>A product&#39;s related products are the configurable, bundle and grouped parents of a simple product, or the variants of a configurable product. Publishing theme sections fires <code>section.update.after</code> for each published section. Imports fire only <code>data_transfer.imports.*</code> events, which none of these listeners handle.</p><p>Targeted forgets go through <code>Webkul\\FPC\\Concerns\\ForgetsPages</code>. It forgets each path on the current host and on every channel&#39;s own hostname, under every locale and currency combination of every channel, because each is a separate entry. <code>ResponseCache::forget()</code> from Spatie builds a single key from the current request&#39;s channel, locale and currency, so from the admin or the console it misses the other variants.</p><h3 id="invalidating-pages-from-your-package" tabindex="-1">Invalidating Pages from Your Package <a class="header-anchor" href="#invalidating-pages-from-your-package" aria-label="Permalink to &quot;Invalidating Pages from Your Package&quot;">​</a></h3><p>When your package changes what a cached page shows, fire your own event and forget the pages in a listener that uses <code>ForgetsPages</code>:</p><p><strong>File:</strong> <code>packages/Webkul/Blog/src/Listeners/PageCache.php</code></p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">php</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Webkul\\Blog\\Listeners</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\FPC\\Concerns\\ForgetsPages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PageCache</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ForgetsPages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * Forget the post&#39;s page and the home page that lists the latest posts.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> afterPostUpdate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($post)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">forgetPages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">homePath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &#39;/</span><span style="--shiki-light:#032F62;--shiki-dark:#DBEDFF;">blog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$post</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">slug,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Map the event in the <code>$listen</code> array of the package&#39;s <code>EventServiceProvider</code>, as <code>&#39;blog.post.update.after&#39; =&gt; [[PageCache::class, &#39;afterPostUpdate&#39;]]</code>, and register that provider from the package&#39;s main provider with <code>$this-&gt;app-&gt;register(EventServiceProvider::class)</code>, as <code>FPCServiceProvider</code> does; <a href="./event-listeners.html#register-a-listener">Event Listeners</a> shows the full provider. Dispatch <code>blog.post.update.after</code> after the post is saved. Your own storefront routes are only cached when you add <code>cache.response</code> to them.</p><h2 id="image-cache" tabindex="-1">Image Cache <a class="header-anchor" href="#image-cache" aria-label="Permalink to &quot;Image Cache&quot;">​</a></h2><p><code>Webkul\\ImageCache</code> keeps nothing on the server. It resizes the stored file on every request to <code>/cache/{template}/{path}</code> and sends an <code>ETag</code> and <code>Cache-Control: max-age={imagecache.lifetime × 60}, public</code>, 30 days by default. Browsers and any CDN in front hold the copies, so there&#39;s nothing to clear, and a changed template reaches a client only when its copy expires. Templates, the route and the configuration are on <a href="./../theme-development/image-cache-templates.html">Image Cache</a>.</p><h2 id="framework-caches" tabindex="-1">Framework Caches <a class="header-anchor" href="#framework-caches" aria-label="Permalink to &quot;Framework Caches&quot;">​</a></h2><p><code>php artisan optimize</code> caches the configuration, events, routes and views, and <code>php artisan optimize:clear</code> removes them and also runs <code>cache:clear</code>. The <strong>Cache Management</strong> configuration page runs the same commands through <code>Webkul\\Admin\\Services\\CacheManagerService</code>:</p><table tabindex="0"><thead><tr><th>Action (key)</th><th>Runs</th></tr></thead><tbody><tr><td>Rebuild All Cache (<code>build-all</code>)</td><td><code>optimize</code></td></tr><tr><td>Cache Config, Cache Routes, Cache Views (<code>build-config</code>, <code>build-routes</code>, <code>build-views</code>)</td><td><code>config:cache</code>, <code>route:cache</code>, <code>view:cache</code></td></tr><tr><td>Clear All Cache (<code>clear-all</code>)</td><td><code>optimize:clear</code>, then <code>responsecache:clear</code></td></tr><tr><td>Clear Config, Application, Compiled, Event, Route and View Cache (<code>clear-config</code>, <code>clear-cache</code>, <code>clear-compiled</code>, <code>clear-events</code>, <code>clear-routes</code>, <code>clear-views</code>)</td><td><code>config:clear</code>, <code>cache:clear</code>, <code>clear-compiled</code>, <code>event:clear</code>, <code>route:clear</code>, <code>view:clear</code></td></tr><tr><td>Flush Page Cache, in the Full Page Cache settings (<code>clear-page-cache</code>)</td><td><code>responsecache:clear</code></td></tr></tbody></table><p>These run in the web process that handles the click. The configuration, route, event and view caches are files, so on more than one server run <code>optimize</code> on every server as part of the deployment instead.</p><h2 id="things-to-watch" tabindex="-1">Things to Watch <a class="header-anchor" href="#things-to-watch" aria-label="Permalink to &quot;Things to Watch&quot;">​</a></h2><ul><li><strong>Clearing the page cache empties its whole store</strong>, which is why it needs a store of its own; see <a href="./../performance/configure-fpc.html#choose-where-pages-are-stored">Configure Full Page Cache</a>.</li><li><strong>Per-server stores go stale.</strong> A <code>file</code> store, or a <code>database</code> store on a local database, is only cleared on the server that handled the change. More than one web server needs shared stores.</li><li><strong>Write through repositories.</strong> A direct model or query builder write leaves the repository cache, and any page built from it, stale.</li><li><strong>Long-running workers keep their repository cache tokens and their booted configuration.</strong> Restart queue workers and reload Octane after a deployment or a configuration change.</li></ul><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../performance/configure-fpc.html">Configure Full Page Cache</a>: the page cache&#39;s store, lifetime, checks and clearing.</li><li><a href="./../performance/configure-load-balancing.html">Configure Load Balancing</a>: the stores several web servers must share.</li><li><a href="./queue-jobs-scheduling.html#running-workers-in-production">Queues, Jobs and Scheduling</a>: what a long-running worker keeps between jobs.</li></ul>`,51)]))}const u=a(i,[["render",r]]);export{g as __pageData,u as default};
