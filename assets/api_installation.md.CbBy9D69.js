import{_ as t,o as a,a as s,a2 as i}from"./chunks/framework.DCcmcJcL.js";const g=JSON.parse('{"title":"Installation","description":"","frontmatter":{},"headers":[],"relativePath":"api/installation.md","filePath":"api/installation.md","lastUpdated":1790143103000}'),o={name:"api/installation.md"};function n(d,e,r,h,l,p){return a(),s("div",null,e[0]||(e[0]=[i(`<h1 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h1><p>This page installs <code>bagisto/bagisto-api</code>, developed at <a href="https://github.com/bagisto/bagisto-api" target="_blank" rel="noreferrer">github.com/bagisto/bagisto-api</a> rather than in Bagisto core, issues the credentials each API surface needs, and lists the settings and commands you use afterwards.</p><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>The package&#39;s README names the Bagisto release each line is tested against; read the <strong>Requirements</strong> section of the release you install:</p><table tabindex="0"><thead><tr><th>Package line</th><th>Latest release</th><th>README requirement</th></tr></thead><tbody><tr><td><code>bagisto/bagisto-api</code> 2.4.x</td><td>v2.4.4</td><td>Bagisto 2.4.x, tested in CI against 2.4.10</td></tr><tr><td><code>bagisto/bagisto-api</code> 2.3.x</td><td>v2.3.1</td><td>Bagisto 2.3, tested in CI against 2.3.8</td></tr></tbody></table><ul><li><strong>Bagisto 2.5 isn&#39;t named yet.</strong> The package&#39;s <code>composer.json</code> has no constraint on the Bagisto version, and Composer resolves the 2.4.x line against Bagisto 2.5&#39;s dependencies, but no release, README or CHANGELOG names Bagisto 2.5. Install it on a copy of your store before production.</li><li><strong>Start from a clean working tree.</strong> The installer edits files in your application, <a href="#what-the-installer-changes">listed below</a>, so you want to review the diff and be able to roll it back.</li></ul><h2 id="installing-with-composer" tabindex="-1">Installing with Composer <a class="header-anchor" href="#installing-with-composer" aria-label="Permalink to &quot;Installing with Composer&quot;">​</a></h2><ol><li><p>Require the package:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> require</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto/bagisto-api</span></span></code></pre></div><p>Composer brings in API Platform for Laravel (<code>api-platform/laravel</code> and <code>api-platform/graphql</code>, <code>~4.3.8</code> on the 2.4.x line) and the Symfony components they need. Laravel&#39;s package discovery registers <code>Webkul\\BagistoApi\\Providers\\BagistoApiServiceProvider</code>.</p></li><li><p>Run the installer:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto-api-platform:install</span></span></code></pre></div></li><li><p>Rebuild the caches the API reads on every request:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto-api-platform:optimize</span></span></code></pre></div><p>It clears stale caches, rebuilds the configuration and route caches, and warms API Platform&#39;s resource metadata.</p></li><li><p>Open <code>https://your-domain.com/api</code>. The landing page links the Shop and Admin playgrounds.</p></li></ol><h2 id="installing-from-source" tabindex="-1">Installing from Source <a class="header-anchor" href="#installing-from-source" aria-label="Permalink to &quot;Installing from Source&quot;">​</a></h2><details class="details custom-block"><summary>Only When You Want the Repository&#39;s Code Rather than a Tagged Release</summary><ol><li><p>Download the package from GitHub and extract it to <code>packages/Webkul/BagistoApi</code>.</p></li><li><p>Register the service provider in <code>bootstrap/providers.php</code>:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">php</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\BagistoApi\\Providers\\BagistoApiServiceProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    BagistoApiServiceProvider</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span></code></pre></div></li><li><p>Add the namespace to the <code>autoload.psr-4</code> section of the root <code>composer.json</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;autoload&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;psr-4&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;Webkul\\\\BagistoApi\\\\&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;packages/Webkul/BagistoApi/src&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></li><li><p>Install API Platform and the Symfony components the package requires. The constraints below are the ones in the 2.4.4 <code>composer.json</code>; take them from the copy you downloaded:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> require</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -W</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  api-platform/laravel:~4.3.8</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  api-platform/graphql:~4.3.8</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;symfony/property-access:^7.0&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;symfony/property-info:^7.1&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;symfony/serializer:^7.4.9&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;symfony/type-info:^7.3&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;symfony/validator:^7.0&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;symfony/web-link:^7.4&quot;</span></span></code></pre></div></li><li><p>Regenerate the autoloader, run the installer and rebuild the caches:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dump-autoload</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto-api-platform:install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto-api-platform:optimize</span></span></code></pre></div></li></ol></details><h2 id="what-the-installer-changes" tabindex="-1">What the Installer Changes <a class="header-anchor" href="#what-the-installer-changes" aria-label="Permalink to &quot;What the Installer Changes&quot;">​</a></h2><p><code>bagisto-api-platform:install</code> changes your application as well as the database:</p><table tabindex="0"><thead><tr><th>Change</th><th>Where</th></tr></thead><tbody><tr><td>Runs <code>vendor:publish</code> for the package&#39;s service provider</td><td>The paths the package publishes</td></tr><tr><td>Symlinks API Platform&#39;s front-end assets, or copies them where a symlink fails</td><td><code>public/vendor/api-platform</code></td></tr><tr><td>Adds <code>api-platform/laravel</code> to <code>extra.laravel.dont-discover</code>, so the package registers API Platform once</td><td><code>composer.json</code></td></tr><tr><td>Declares <code>TranslatableModel</code> abstract</td><td><code>packages/Webkul/Core/src/Eloquent/TranslatableModel.php</code></td></tr><tr><td>Adds a <code>-&gt;withProviders([...])</code> call with <code>ApiPlatformProvider</code>, <code>ApiPlatformDeferredProvider</code> and <code>ApiPlatformEventProvider</code> before <code>-&gt;create()</code></td><td><code>bootstrap/app.php</code></td></tr><tr><td>Runs <code>migrate</code>, creating <code>storefront_keys</code>, <code>cart_tokens</code>, <code>admin_personal_access_tokens</code> and <code>admin_api_audits</code></td><td>Database</td></tr><tr><td>Generates a storefront key named <code>Default Storefront Key1</code>, saves it as <code>STOREFRONT_PLAYGROUND_KEY</code>, and writes the other <a href="#configuration">storefront settings</a></td><td><code>.env</code></td></tr><tr><td>Copies the package&#39;s API Platform configuration, replacing any existing file</td><td><code>config/api-platform.php</code></td></tr><tr><td>Clears the caches and runs <code>optimize</code></td><td><code>bootstrap/cache</code></td></tr></tbody></table><p>Before most edits the installer checks whether the change is already there and skips it, so running it again is safe. The exception is <code>config/api-platform.php</code>, which it copies again every time.</p><h2 id="credentials" tabindex="-1">Credentials <a class="header-anchor" href="#credentials" aria-label="Permalink to &quot;Credentials&quot;">​</a></h2><p>The two API surfaces authenticate differently, and neither accepts the other&#39;s credential.</p><table tabindex="0"><thead><tr><th>Surface</th><th>Endpoints</th><th>Credential</th></tr></thead><tbody><tr><td>Shop</td><td><code>/api/shop/*</code>, <code>POST /api/graphql</code></td><td><code>X-STOREFRONT-KEY: &lt;key&gt;</code> on every request, plus <code>Authorization: Bearer &lt;token&gt;</code> with a customer or guest-cart token for account, cart and checkout calls</td></tr><tr><td>Admin</td><td><code>/api/admin/*</code>, <code>POST /api/admin/graphql</code></td><td><code>Authorization: Bearer &lt;id&gt;|&lt;token&gt;</code> with an Integration token, and no storefront key</td></tr></tbody></table><h3 id="storefront-keys" tabindex="-1">Storefront Keys <a class="header-anchor" href="#storefront-keys" aria-label="Permalink to &quot;Storefront Keys&quot;">​</a></h3><p>A storefront key identifies the application calling the Shop API, not a person. It ends up in browser bundles and mobile apps, so treat it as public. The installer&#39;s own key is the value of <code>STOREFRONT_PLAYGROUND_KEY</code> in <code>.env</code>, which is enough for the examples on the next pages. For each client, issue a key of its own; <code>bagisto-api:generate-key</code> prints each new key when it creates it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto-api:generate-key</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Mobile App&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rate-limit=5000</span></span></code></pre></div><ul><li><code>--rate-limit</code> defaults to <code>100</code> and is capped at <code>5000</code>; an empty value (<code>--rate-limit=</code>) creates a key with no limit.</li><li>The command&#39;s help calls it requests per minute, but in v2.4.4 both the REST and the GraphQL key checks count it <strong>per hour</strong>, so size it for an hour of traffic.</li><li><code>--no-activation</code> creates the key inactive.</li></ul><p>Manage existing keys with <code>bagisto-api:key:manage</code>. Its first argument is one of <code>rotate</code>, <code>deactivate</code>, <code>cleanup</code>, <code>status</code>, <code>expiring</code>, <code>unused</code> or <code>summary</code>, and <code>--key</code> takes a key&#39;s ID or name:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto-api:key:manage</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --key=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Mobile App&quot;</span></span></code></pre></div><h3 id="customer-and-cart-tokens" tabindex="-1">Customer and Cart Tokens <a class="header-anchor" href="#customer-and-cart-tokens" aria-label="Permalink to &quot;Customer and Cart Tokens&quot;">​</a></h3><p>The Shop API issues these itself: customer sign-in returns a <code>token</code>, and creating a guest cart returns a cart token. <a href="./rest-api.html">REST API</a> and <a href="./graphql-api.html">GraphQL API</a> show both calls.</p><h3 id="integration-tokens" tabindex="-1">Integration Tokens <a class="header-anchor" href="#integration-tokens" aria-label="Permalink to &quot;Integration Tokens&quot;">​</a></h3><p>An admin generates an Integration token in the admin; there is no sign-in endpoint or Artisan command for it. Each token belongs to one admin user, can do no more than that admin&#39;s role allows, is shown only once, and must stay on a server. The package README&#39;s <a href="https://github.com/bagisto/bagisto-api#admin-api-authentication" target="_blank" rel="noreferrer">Admin API Authentication</a> section has the steps. The Integration screens appear only while the package&#39;s <code>api.integration.settings.enabled</code> configuration field is on, which is the default.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>The installer writes these variables to <code>.env</code>. They feed the <code>storefront</code> configuration the package merges:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Config key</th><th>Default</th><th>Controls</th></tr></thead><tbody><tr><td><code>STOREFRONT_DEFAULT_RATE_LIMIT</code></td><td><code>storefront.default_rate_limit</code></td><td><code>100</code></td><td>The value the REST <code>X-RateLimit-Limit</code> header reports; the limit applied is each key&#39;s own rate limit</td></tr><tr><td><code>STOREFRONT_CACHE_TTL</code></td><td><code>storefront.cache_ttl</code></td><td><code>60</code></td><td>Minutes a key lookup stays cached</td></tr><tr><td><code>STOREFRONT_KEY_PREFIX</code></td><td><code>storefront.key_prefix</code></td><td><code>storefront_key_</code></td><td>Prefix of those cache entries</td></tr><tr><td><code>STOREFRONT_PLAYGROUND_KEY</code></td><td><code>storefront.playground_key</code></td><td>none</td><td>Key the Swagger UI and GraphiQL pages use</td></tr><tr><td><code>API_PLAYGROUND_AUTO_INJECT_STOREFRONT_KEY</code></td><td><code>storefront.auto_inject_playground_key</code></td><td><code>false</code></td><td>Fills that key in on the playground pages</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">Keep Auto-Inject Off on Public Stores</p><p>With <code>API_PLAYGROUND_AUTO_INJECT_STOREFRONT_KEY</code> on, the playground key is handed to anyone who opens <code>/api/shop</code> or <code>/api/graphiql</code>. Turn it on only in a local or private environment.</p></div><p>API Platform&#39;s own settings are in <code>config/api-platform.php</code>, including <code>swagger_ui.enabled</code> and <code>graphql.graphiql.enabled</code>, which turn the Swagger UI and GraphiQL pages on or off, and the GraphQL limits <code>graphql.max_query_complexity</code> and <code>graphql.max_query_depth</code>. Run <code>php artisan bagisto-api-platform:optimize</code> after changing either file.</p><h2 id="where-things-are-on-your-store" tabindex="-1">Where Things Are on Your Store <a class="header-anchor" href="#where-things-are-on-your-store" aria-label="Permalink to &quot;Where Things Are on Your Store&quot;">​</a></h2><table tabindex="0"><thead><tr><th>URL</th><th>What it serves</th></tr></thead><tbody><tr><td><code>/api</code></td><td>Landing page linking both surfaces</td></tr><tr><td><code>/api/shop</code></td><td>Swagger UI for the Shop REST API</td></tr><tr><td><code>/api/shop/docs</code></td><td>The Shop OpenAPI document, as JSON</td></tr><tr><td><code>/api/admin</code></td><td>Swagger UI for the Admin REST API</td></tr><tr><td><code>/api/admin/docs</code></td><td>The Admin OpenAPI document, as JSON</td></tr><tr><td><code>/api/graphql</code></td><td>Shop GraphQL endpoint</td></tr><tr><td><code>/api/graphiql</code></td><td>GraphiQL for the Shop schema</td></tr><tr><td><code>/api/admin/graphql</code></td><td>Admin GraphQL endpoint</td></tr><tr><td><code>/api/admin/graphiql</code></td><td>GraphiQL for the Admin schema</td></tr></tbody></table><p>The endpoint reference, with every request and response, is at <a href="https://api-docs.bagisto.com/" target="_blank" rel="noreferrer">api-docs.bagisto.com</a>. Its <a href="https://api-docs.bagisto.com/llms.txt" target="_blank" rel="noreferrer"><code>llms.txt</code></a> indexes every endpoint for AI tools.</p><h2 id="artisan-commands" tabindex="-1">Artisan Commands <a class="header-anchor" href="#artisan-commands" aria-label="Permalink to &quot;Artisan Commands&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>Does</th></tr></thead><tbody><tr><td><code>bagisto-api-platform:install</code></td><td>Installs and configures the package, as described above</td></tr><tr><td><code>bagisto-api-platform:optimize</code></td><td>Clears stale caches, rebuilds the configuration and route caches, and warms the API Platform metadata cache</td></tr><tr><td><code>bagisto-api-platform:clear-cache</code></td><td>Flushes the API Platform metadata and schema cache; <code>--store=</code> names a different cache store</td></tr><tr><td><code>bagisto-api-platform:warm-cache</code></td><td>Builds the resource metadata cache ahead of the first request</td></tr><tr><td><code>bagisto-api-platform:export-schema</code></td><td>Writes each surface&#39;s OpenAPI JSON and GraphQL SDL; <code>--path=</code> sets the directory and <code>--transport=</code> is <code>all</code>, <code>rest</code> or <code>graphql</code></td></tr><tr><td><code>bagisto-api:generate-key</code></td><td>Creates a storefront key</td></tr><tr><td><code>bagisto-api:key:manage</code></td><td>Rotates, deactivates and reports on storefront keys</td></tr><tr><td><code>bagisto-api:key:maintain</code></td><td>Runs key maintenance with <code>--cleanup</code>, <code>--invalidate</code>, <code>--notify</code> or <code>--all</code></td></tr><tr><td><code>bagisto-api:prune-audits</code></td><td>Deletes Admin API audit history older than <code>bagistoapi.audit.retention_days</code>; <code>--days=</code> overrides it</td></tr><tr><td><code>bagisto-api:prune-cart-uploads</code></td><td>Deletes abandoned custom-option uploads; the package schedules it every two hours</td></tr></tbody></table><h2 id="adding-your-own-endpoints" tabindex="-1">Adding Your Own Endpoints <a class="header-anchor" href="#adding-your-own-endpoints" aria-label="Permalink to &quot;Adding Your Own Endpoints&quot;">​</a></h2><p>The package documents a supported way to add REST and GraphQL endpoints from your own Bagisto package without editing it. You write resource classes marked <code>#[ApiResource]</code>, with providers and processors tagged in the container. Your service provider then merges your resource directory into the <code>api-platform.resources</code> config. Follow <a href="https://api-docs.bagisto.com/api/for-developers/" target="_blank" rel="noreferrer">For Developers</a> on the API documentation site, starting with <a href="https://api-docs.bagisto.com/api/for-developers/register-your-package" target="_blank" rel="noreferrer">Register Your Package</a>. Changes made inside <code>vendor/bagisto/bagisto-api</code> are lost on the next <code>composer update</code>.</p><h2 id="things-to-watch" tabindex="-1">Things to Watch <a class="header-anchor" href="#things-to-watch" aria-label="Permalink to &quot;Things to Watch&quot;">​</a></h2><ul><li><strong>An upgrade can undo the installer.</strong> Two of its edits are in files Bagisto itself ships, <code>packages/Webkul/Core/src/Eloquent/TranslatableModel.php</code> and <code>bootstrap/app.php</code>. After an upgrade that replaces either file, run <code>php artisan bagisto-api-platform:install</code> again, then <code>php artisan bagisto-api-platform:optimize</code>.</li><li><strong>The migration step can stop in production.</strong> The installer runs <code>php artisan migrate</code> without <code>--force</code>. With <code>APP_ENV=production</code>, Laravel asks for confirmation, which the installer&#39;s child process can&#39;t give, so the migration is cancelled and the installer stops at that step. Run the installer on a non-production copy and commit the files it changes. Then, on production, run <code>php artisan migrate --force</code> and issue a key there with <code>bagisto-api:generate-key</code>.</li><li><strong>The route cache hides new endpoints.</strong> After installing or updating the package, or adding an endpoint, run <code>bagisto-api-platform:optimize</code>. Until then the cached route table doesn&#39;t know about the change.</li><li><strong>Cart upload pruning needs the scheduler.</strong> Run Laravel&#39;s scheduler as described in <a href="./../advanced/queue-jobs-scheduling.html">Queues, Jobs and Scheduling</a>.</li><li><strong>Exported schemas default to the package folder.</strong> Without <code>--path</code>, <code>export-schema</code> writes to the package&#39;s <code>schema/generated</code> directory. On a Composer install that is <code>vendor/bagisto/bagisto-api/schema/generated</code>, which the next update replaces.</li><li><strong>Removing the package leaves its edits behind.</strong> <code>composer remove bagisto/bagisto-api</code> also removes API Platform, but <code>bootstrap/app.php</code> still names API Platform&#39;s providers. Take the <code>withProviders</code> block out of <code>bootstrap/app.php</code> before removing the package. The package&#39;s tables and <code>.env</code> lines stay until you remove them.</li></ul><h2 id="next-step" tabindex="-1">Next Step <a class="header-anchor" href="#next-step" aria-label="Permalink to &quot;Next Step&quot;">​</a></h2><p>Make your first calls with the <a href="./rest-api.html">REST API</a>, or go straight to the <a href="./graphql-api.html">GraphQL API</a>.</p>`,43)]))}const k=t(o,[["render",n]]);export{g as __pageData,k as default};
