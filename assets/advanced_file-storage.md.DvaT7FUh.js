import{_ as s,o as a,a as i,a2 as t}from"./chunks/framework.DCcmcJcL.js";const k=JSON.parse('{"title":"File Storage","description":"","frontmatter":{},"headers":[],"relativePath":"advanced/file-storage.md","filePath":"advanced/file-storage.md","lastUpdated":1789464754000}'),n={name:"advanced/file-storage.md"};function o(d,e,l,r,h,p){return a(),i("div",null,e[0]||(e[0]=[t(`<h1 id="file-storage" tabindex="-1">File Storage <a class="header-anchor" href="#file-storage" aria-label="Permalink to &quot;File Storage&quot;">​</a></h1><p>Product images, category images, theme section media, editor uploads and most other files Bagisto writes go to Laravel&#39;s <strong>default filesystem disk</strong>: the local <code>public</code> disk out of the box, or Amazon S3 or Cloudflare R2 chosen in the admin. This page explains the disks, how the admin setting overrides them, which files stay on a local disk whatever the default, and how images are converted and resized.</p><h2 id="disks" tabindex="-1">Disks <a class="header-anchor" href="#disks" aria-label="Permalink to &quot;Disks&quot;">​</a></h2><p><strong>File:</strong> <code>config/filesystems.php</code></p><table tabindex="0"><thead><tr><th>Disk</th><th>Driver and root</th><th>Used for</th></tr></thead><tbody><tr><td><code>public</code></td><td>local, <code>storage/app/public</code></td><td>The default disk: catalog and theme media, customer uploads, sitemaps</td></tr><tr><td><code>private</code></td><td>local, <code>storage/app/private</code></td><td>Import files and downloadable product links; never served directly</td></tr><tr><td><code>local</code></td><td>local, <code>storage/app</code></td><td>Laravel&#39;s default disk, not used by core</td></tr><tr><td><code>s3</code></td><td>S3, from the <code>AWS_*</code> keys</td><td>Amazon S3 or another S3-compatible service</td></tr><tr><td><code>r2</code></td><td>S3, from the <code>R2_*</code> keys</td><td>Cloudflare R2, with path-style endpoints on by default</td></tr></tbody></table><p>The default is <code>env(&#39;FILESYSTEM_DISK&#39;, &#39;public&#39;)</code>, and <code>links</code> maps <code>public/storage</code> to <code>storage/app/public</code> for <code>php artisan storage:link</code> (and <code>storage:unlink</code>). The link is needed while media are served from the local <code>public</code> disk, and for sitemaps whatever the default disk is.</p><h3 id="files-that-stay-on-a-local-disk" tabindex="-1">Files That Stay on a Local Disk <a class="header-anchor" href="#files-that-stay-on-a-local-disk" aria-label="Permalink to &quot;Files That Stay on a Local Disk&quot;">​</a></h3><p>Some code names its disk, so these files don&#39;t follow the default:</p><table tabindex="0"><thead><tr><th>Files</th><th>Disk</th><th>Written by</th></tr></thead><tbody><tr><td>Import files, validation fragments, downloaded import images, error reports</td><td><code>private</code></td><td><code>Webkul\\DataTransfer\\Helpers\\Import</code>, the importer concerns <code>ValidatesInChunks</code> and <code>DownloadsImages</code>, the CSV, XLS, XLSX and XML sources, and <code>ImportController</code></td></tr><tr><td>Downloadable product link files and link samples</td><td><code>private</code></td><td><code>Webkul\\Product\\Repositories\\ProductDownloadableLinkRepository</code>, served by the Shop <code>DownloadableProductController</code> and <code>ProductController::downloadSample()</code></td></tr><tr><td>Sitemaps</td><td><code>public</code></td><td><code>Webkul\\Sitemap\\Jobs\\ProcessSitemap</code> with <code>writeToDisk(&#39;public&#39;, …)</code>, linked under <code>/storage/</code> by the Shop <code>SitemapController</code></td></tr></tbody></table><p>The import pipeline reads its files by path, which a remote adapter can&#39;t provide. Everything else, including product images and videos, category logos and banners, theme section images, downloadable samples, locale logos, search images, review attachments and editor uploads, uses the default disk and moves with it. On more than one server, share <code>storage/app/private</code> and <code>storage/app/public</code>; see <a href="./../performance/configure-load-balancing.html">Configure Load Balancing</a>.</p><h2 id="choosing-the-disk-in-the-admin" tabindex="-1">Choosing the Disk in the Admin <a class="header-anchor" href="#choosing-the-disk-in-the-admin" aria-label="Permalink to &quot;Choosing the Disk in the Admin&quot;">​</a></h2><p>The <strong>File Management</strong> configuration section stores these settings. Bagisto 2.4 has no such section, no <code>r2</code> disk and no <code>league/flysystem-aws-s3-v3</code> adapter; there, require the adapter yourself and set <code>FILESYSTEM_DISK=s3</code> in <code>.env</code>.</p><table tabindex="0"><thead><tr><th>Settings</th><th>Keys</th></tr></thead><tbody><tr><td><code>file_management.general.settings</code></td><td><code>default_driver</code>: <code>public</code> (the default), <code>s3</code> or <code>r2</code></td></tr><tr><td><code>file_management.amazon_s3.settings</code></td><td><code>key</code>, <code>secret</code>, <code>region</code>, <code>bucket</code>, <code>url</code>, <code>endpoint</code>, <code>use_path_style_endpoint</code></td></tr><tr><td><code>file_management.cloudflare_r2.settings</code></td><td><code>account_id</code>, <code>key</code>, <code>secret</code>, <code>bucket</code>, <code>url</code></td></tr></tbody></table><p><code>Webkul\\Core\\Filesystem\\StorageConfigurator::configure()</code> runs first in <code>CoreServiceProvider::boot()</code>, before anything resolves storage. It reads the <code>file_management.*</code> rows straight from <code>core_config</code>, because the channel-aware configuration reader isn&#39;t available that early, and then:</p><ol><li>Returns without changing anything when the table can&#39;t be read (during installation) or no <code>default_driver</code> is stored, so <code>FILESYSTEM_DISK</code> stays in charge.</li><li>Resolves the driver with <code>Webkul\\Core\\Enums\\SupportedFilesystemEnum</code>, and returns without changing anything when the driver&#39;s adapter class (<code>League\\Flysystem\\AwsS3V3\\AwsS3V3Adapter</code>) isn&#39;t installed.</li><li>Copies every non-empty setting onto <code>filesystems.disks.s3.*</code> or <code>filesystems.disks.r2.*</code>. A setting left empty keeps its <code>.env</code> value. For R2 it builds the endpoint from the account id as <code>https://{account_id}.r2.cloudflarestorage.com</code>, and always sets the region to <code>auto</code> and path-style endpoints on.</li><li>Sets <code>filesystems.default</code> to the driver. Once a driver is stored it wins over <code>FILESYSTEM_DISK</code>, even when the stored driver is <code>public</code>.</li></ol><p>Because this happens at boot, a queue worker keeps the disk it booted with until <code>php artisan queue:restart</code>, and an <a href="./../performance/configure-laravel-octane.html">Octane</a> worker until <code>php artisan octane:reload</code>.</p><div class="warning custom-block"><p class="custom-block-title">Existing Files Don&#39;t Move</p><p>Switching the default disk changes where new files are written. Files already uploaded stay where they are, and their paths are stored without a disk name, so after a switch to S3 a product image uploaded earlier is looked for on S3 and not found. Copy <code>storage/app/public</code> to the bucket before switching, or switch before anything is uploaded.</p></div><h3 id="environment-keys" tabindex="-1">Environment Keys <a class="header-anchor" href="#environment-keys" aria-label="Permalink to &quot;Environment Keys&quot;">​</a></h3><p><code>.env.example</code> carries the keys for both remote disks:</p><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FILESYSTEM_DISK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=public</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_ACCESS_KEY_ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_SECRET_ACCESS_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_DEFAULT_REGION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=us-east-1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_BUCKET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_USE_PATH_STYLE_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=false</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_ACCESS_KEY_ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_SECRET_ACCESS_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_DEFAULT_REGION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=auto</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_BUCKET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_USE_PATH_STYLE_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=true</span></span></code></pre></div><p>To keep credentials out of the database, set them here and leave the matching admin fields empty; only <code>default_driver</code> needs to be stored. The Amazon S3 path-style switch (<code>use_path_style_endpoint</code>) is the exception: it&#39;s stored whenever the Amazon S3 settings are saved, and from then on overrides <code>AWS_USE_PATH_STYLE_ENDPOINT</code>.</p><div class="warning custom-block"><p class="custom-block-title">Fill in the URL</p><p>Fill in <strong>URL</strong> in the admin, or <code>AWS_URL</code> or <code>R2_URL</code>, with the bucket&#39;s public or CDN address. An empty <code>AWS_URL=</code> line still counts as set, so <code>Storage::url()</code> returns a bare <code>/path</code> on your own host and every file link breaks. On S3 you can instead delete the line, and the adapter builds the bucket&#39;s own object URLs.</p></div><h2 id="urls-on-a-remote-disk" tabindex="-1">URLs on a Remote Disk <a class="header-anchor" href="#urls-on-a-remote-disk" aria-label="Permalink to &quot;URLs on a Remote Disk&quot;">​</a></h2><ul><li><strong>Resized images.</strong> <code>image_urls()</code> always returns <code>cache/{template}/{path}</code> URLs, and <code>Webkul\\ImageCache\\Http\\Controllers\\ImageCacheController</code> reads the file with <code>Storage::get()</code> from the default disk before trying the local paths in <code>config/imagecache.php</code>. Product, category and swatch images are resized whichever disk holds them. On Bagisto 2.4 a remote disk linked each size to the original file.</li><li><strong>Theme media.</strong> <code>bagisto_theme_storage()</code> returns <code>Webkul\\Theme\\ThemeStorage</code>, which resolves a stored section path when the page renders: <code>url()</code> gives a URL on the current host for a local disk and <code>Storage::url($path)</code> for a remote one, and <code>resizedUrl()</code> gives the <code>cache/</code> URL. Section paths in <code>theme_section_translations</code> are stored bare, without host or <code>storage/</code> prefix, so a change of domain or disk doesn&#39;t break them. The migrations <code>2026_08_25_000001_drop_storage_prefix_from_theme_section_paths</code> and <code>2026_08_25_000002_make_theme_section_urls_portable</code> rewrite older rows into that form.</li><li><strong>Other files.</strong> <code>Storage::url($path)</code> on the <code>s3</code> or <code>r2</code> disk builds the URL from the <code>url</code> setting whenever it&#39;s set, even to an empty string, so point it at your CDN or bucket domain.</li></ul><h2 id="image-processing" tabindex="-1">Image Processing <a class="header-anchor" href="#image-processing" aria-label="Permalink to &quot;Image Processing&quot;">​</a></h2><p>Uploaded images are re-encoded to WebP, at Laravel&#39;s default quality of 70, when they&#39;re stored, with <code>image_manager()-&gt;fromUpload($file)-&gt;toWebp()-&gt;toBytes()</code>:</p><table tabindex="0"><thead><tr><th>Upload</th><th>Code</th></tr></thead><tbody><tr><td>Product images (any upload whose MIME type contains <code>image</code>)</td><td><code>Webkul\\Product\\Repositories\\ProductMediaRepository</code></td></tr><tr><td>Category logos and banners</td><td><code>Webkul\\Category\\Repositories\\CategoryRepository</code></td></tr><tr><td>Theme section images</td><td><code>Webkul\\Theme\\Repositories\\SectionRepository</code></td></tr><tr><td>Product images from an import</td><td><code>Webkul\\DataTransfer\\Helpers\\Importers\\Product\\Importer</code></td></tr></tbody></table><p>Other uploads are stored as sent. <code>image_manager()</code> returns Laravel&#39;s <code>Illuminate\\Image\\ImageManager</code>, and <code>config/images.php</code> chooses the driver with <code>IMAGE_DRIVER</code> (<code>gd</code> by default, or <code>imagick</code>).</p><p>Resizing happens at request time, at <code>/cache/{template}/{path}</code>, and nothing is written back to a disk, so in production put a CDN or reverse proxy in front of <code>/cache/</code>. The templates, the response headers and theme overrides are on <a href="./../theme-development/image-cache-templates.html">Image Cache</a>.</p><h2 id="writing-files-from-a-package" tabindex="-1">Writing Files from a Package <a class="header-anchor" href="#writing-files-from-a-package" aria-label="Permalink to &quot;Writing Files from a Package&quot;">​</a></h2><p>Store files a store owner may want on object storage on the default disk, and working files that must be read by path on the <code>private</code> disk. Pass SVG uploads through <code>Webkul\\Core\\Traits\\Sanitizer</code>, whose <code>sanitizeSVG($path, $mimeType)</code> cleans an <code>image/svg</code> or <code>image/svg+xml</code> file in place with <code>enshrined/svg-sanitize</code> and removes remote references; core does this for locale logos, search images, review attachments and editor uploads.</p><p><strong>File:</strong> <code>packages/Webkul/Blog/src/Repositories/PostAttachmentRepository.php</code></p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">php</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Webkul\\Blog\\Repositories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Illuminate\\Http\\UploadedFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\Blog\\Contracts\\PostAttachment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\Core\\Eloquent\\Repository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\Core\\Traits\\Sanitizer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PostAttachmentRepository</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Repository</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Sanitizer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * Specify model class name.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PostAttachment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * Store an uploaded file for a post on the default disk.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> upload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">UploadedFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $file, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $postId)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        $path </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;blog/&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$postId);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sanitizeSVG</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($path, $file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getMimeType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &#39;post_id&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $postId,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &#39;path&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $path,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>store()</code> without a disk name writes to the default disk. For a file that must stay local, pass <code>&#39;private&#39;</code> as the second argument, and don&#39;t pass that file to <code>sanitizeSVG()</code>, which reads and writes the default disk. Resolve a public URL with <code>Storage::url($path)</code>, or build resized URLs with <code>image_urls($path)</code>.</p><h2 id="things-to-watch" tabindex="-1">Things to Watch <a class="header-anchor" href="#things-to-watch" aria-label="Permalink to &quot;Things to Watch&quot;">​</a></h2><ul><li><strong>Switching disks strands existing files</strong> until you copy them across.</li><li><strong>The <code>private</code> disk is always local</strong>, so imports and downloadable products need a shared <code>storage/app/private</code> on more than one server.</li><li><strong>Resizing costs a request each time</strong> without a CDN or proxy cache in front of <code>/cache/</code>.</li></ul><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./../performance/configure-load-balancing.html#files">Configure Load Balancing</a>: the storage directories several web servers must share.</li><li><a href="./../theme-development/image-cache-templates.html">Image Cache</a>: how images are resized and served, and adding templates.</li></ul>`,38)]))}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
