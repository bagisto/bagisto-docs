import{_ as s,o as a,a as t,a2 as i}from"./chunks/framework.DCcmcJcL.js";const k=JSON.parse('{"title":"Installation","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/installation.md","filePath":"getting-started/installation.md","lastUpdated":1790143103000}'),n={name:"getting-started/installation.md"};function o(l,e,d,h,r,p){return a(),t("div",null,e[0]||(e[0]=[i(`<h1 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h1><p>This page installs Bagisto with Composer and gets it running on your machine. Check the <a href="./before-you-start.html#system-requirements">system requirements</a> first, and create an empty database for the store. Git, the web installer and Docker are covered after the main path, under <a href="#other-ways-to-install">Other Ways to Install</a> and <a href="#run-with-docker">Run with Docker</a>.</p><p><a id="🚀-quick-installation-recommended"></a></p><h2 id="install-with-composer" tabindex="-1">Install with Composer <a class="header-anchor" href="#install-with-composer" aria-label="Permalink to &quot;Install with Composer&quot;">​</a></h2><ol><li><p>Create the project:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create-project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto/bagisto</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-bagisto-store</span></span></code></pre></div><p>Composer installs the latest stable release, copies <code>.env.example</code> to <code>.env</code> and generates <code>APP_KEY</code>. To install a particular release line, add a version constraint, for example <code>composer create-project bagisto/bagisto my-bagisto-store &quot;2.5.*&quot;</code>.</p></li><li><p>Go to the project directory:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-bagisto-store</span></span></code></pre></div></li><li><p>Run the installer and answer its questions:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto:install</span></span></code></pre></div><p>When it asks for the application URL, enter <code>http://localhost:8000</code>, the address <code>php artisan serve</code> uses in step 4.</p></li><li><p>Start the development server:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span></span></code></pre></div><p>Open the storefront at <code>http://localhost:8000</code> and the admin at <code>http://localhost:8000/admin</code>, with the admin account you created. <a href="#run-bagisto-locally">Run Bagisto Locally</a> has the details.</p></li></ol><div class="danger custom-block"><p class="custom-block-title">Fresh Installs Only</p><p><code>bagisto:install</code> runs <code>db:wipe</code> and <code>migrate:fresh</code>, which delete everything in the database. Never run it on a store that has data; upgrades use <code>php artisan migrate</code>, as the <a href="./upgrade-guide.html">Upgrade Guide</a> describes.</p></div><h3 id="what-the-installer-asks" tabindex="-1">What the Installer Asks <a class="header-anchor" href="#what-the-installer-asks" aria-label="Permalink to &quot;What the Installer Asks&quot;">​</a></h3><ul><li><strong>Application:</strong> name, URL, timezone, default locale and currency, and the locales and currencies your channels allow. The URL offered is <code>APP_URL</code> from <code>.env</code>, which is <code>http://localhost</code> in a new project.</li><li><strong>Database:</strong> MySQL, MariaDB or PostgreSQL, host, port, database name, an optional table prefix of up to four letters, digits or underscores, username and password. The host and port offered come from <code>.env</code>, so the port stays <code>3306</code> unless you change it; enter <code>5432</code> for PostgreSQL.</li><li><strong>Admin account:</strong> name, email, and a password of at least six characters.</li><li><strong>Sample products:</strong> whether to add the demo catalog.</li></ul><details class="details custom-block"><summary>What the Installer Does with Your Answers</summary><p>The installer writes your answers to <code>.env</code>, generates the application key, wipes the database and runs every migration, seeds the channel, locales, currencies and other basic data, links the <code>storage/</code> directory, creates the admin account, adds and indexes the sample products if you asked for them, and clears the caches. It prints the admin URL and the credentials, and last asks whether to open the Bagisto Cloud hosting page in your browser.</p></details><h2 id="run-bagisto-locally" tabindex="-1">Run Bagisto Locally <a class="header-anchor" href="#run-bagisto-locally" aria-label="Permalink to &quot;Run Bagisto Locally&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Page</th><th>Address</th></tr></thead><tbody><tr><td>Storefront</td><td><code>http://localhost:8000</code></td></tr><tr><td>Admin</td><td><code>http://localhost:8000/admin</code>, which redirects to <code>/admin/login</code></td></tr><tr><td>Customer registration</td><td><code>http://localhost:8000/customer/register</code></td></tr></tbody></table><ul><li><strong><code>APP_URL</code></strong> in <code>.env</code> must equal the address you open, including the port; image URLs and links are built from it. The admin prefix comes from <code>APP_ADMIN_URL</code>.</li><li><strong>No npm step.</strong> The admin, storefront and installer assets are already built and committed under <code>public/themes/</code>. To work on those assets, see <a href="./../architecture/frontend.html#build-commands">Build Commands</a>.</li><li><strong>Queued work runs inline.</strong> <code>.env.example</code> sets <code>QUEUE_CONNECTION=sync</code>, so mail and search indexing run inside the request. That&#39;s fine on your machine; a production store needs a queue worker, as <a href="./deployment.html">Deployment</a> explains.</li><li><strong>Prefer <code>php artisan serve</code> to <code>php artisan dev</code></strong> until you have set up Octane; see <a href="#use-php-artisan-dev">Things to Watch</a>.</li></ul><h2 id="other-ways-to-install" tabindex="-1">Other Ways to Install <a class="header-anchor" href="#other-ways-to-install" aria-label="Permalink to &quot;Other Ways to Install&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Use it when</th></tr></thead><tbody><tr><td><a href="#install-without-prompts">Install Without Prompts</a></td><td>You script the install, for example in CI</td></tr><tr><td><a href="#install-step-by-step">Install Step by Step</a></td><td>You want to run each installer step yourself</td></tr><tr><td><a href="#install-from-git">Install from Git</a></td><td>You want to track releases or contribute to Bagisto</td></tr><tr><td><a href="#use-the-web-installer">Web installer</a></td><td>You prefer to answer the setup questions in a browser</td></tr><tr><td><a href="#run-with-docker">Docker</a></td><td>You want a running store without installing PHP, or a containerized development setup</td></tr></tbody></table><h3 id="install-without-prompts" tabindex="-1">Install Without Prompts <a class="header-anchor" href="#install-without-prompts" aria-label="Permalink to &quot;Install Without Prompts&quot;">​</a></h3><p>With Laravel&#39;s global <code>--no-interaction</code> option, the installer asks nothing:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto:install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-interaction</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --demo-samples</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Option</th><th>Effect</th></tr></thead><tbody><tr><td><code>-n</code>, <code>--no-interaction</code></td><td>Uses the settings already in <code>.env</code>, creates the default admin account (<code>admin@example.com</code> / <code>admin123</code>) and skips sample products</td></tr><tr><td><code>--demo-samples</code></td><td>Adds and indexes the sample products. In interactive mode, it adds them whatever you answer.</td></tr></tbody></table><p>Set the <code>DB_*</code> values in <code>.env</code> first. <a href="./../advanced/artisan-commands.html#bagisto-install">Artisan Commands</a> has the full reference.</p><div class="warning custom-block"><p class="custom-block-title">Not with <code>APP_ENV=production</code></p><p>In production, <code>db:wipe</code> and <code>migrate:fresh</code> ask for confirmation, and <code>--no-interaction</code> answers no. The installer then carries on without creating any tables. Install before you set <code>APP_ENV=production</code>, or follow the <a href="./deployment.html#production-checklist">production checklist</a> on a production server.</p></div><h3 id="install-step-by-step" tabindex="-1">Install Step by Step <a class="header-anchor" href="#install-step-by-step" aria-label="Permalink to &quot;Install Step by Step&quot;">​</a></h3><p>Configure <code>.env</code>, then run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> key:generate</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate:fresh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --seed</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> storage:link</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> optimize:clear</span></span></code></pre></div><p>The seeders create the default channel, locales, currencies and attribute families, and the admin account <code>admin@example.com</code> / <code>admin123</code>, but no products. To add the sample catalog:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db:seed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --class=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Webkul\\Installer\\Database\\Seeders\\ProductTableSeeder&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> indexer:index</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode=full</span></span></code></pre></div><h3 id="install-from-git" tabindex="-1">Install from Git <a class="header-anchor" href="#install-from-git" aria-label="Permalink to &quot;Install from Git&quot;">​</a></h3><p>Clone the repository, check out the release you want, and install the dependencies:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/bagisto/bagisto.git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-bagisto-store</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-bagisto-store</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tag</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --list</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> checkout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">release-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto:install</span></span></code></pre></div><p>A clone has no <code>.env</code> file; <code>bagisto:install</code> copies <code>.env.example</code> and generates the key itself. You can also <a href="https://bagisto.com/en/download/" target="_blank" rel="noreferrer">download a release</a> from the Bagisto website and run <code>composer install</code> in it.</p><h3 id="use-the-web-installer" tabindex="-1">Use the Web Installer <a class="header-anchor" href="#use-the-web-installer" aria-label="Permalink to &quot;Use the Web Installer&quot;">​</a></h3><p>Until Bagisto is installed, <code>Webkul\\Installer\\Http\\Middleware\\CanInstall</code> redirects every request to <code>/install</code>. Bagisto counts as installed once <code>storage/installed</code> exists, or once <code>.env</code> exists and the database&#39;s <code>admins</code> table has at least one row.</p><ol><li>Get the code with <a href="#install-with-composer">Composer</a> (steps 1 and 2), or with <a href="#install-from-git">Git</a> followed by <code>cp .env.example .env</code> and <code>php artisan key:generate</code>. Don&#39;t run <code>bagisto:install</code>. The installer&#39;s pages need an application key before they can load.</li><li>Serve the <code>public/</code> directory, with your web server or with <code>php artisan serve</code>.</li><li>Open the site in a browser. The installer checks the PHP version and extensions, asks for the application and database settings, runs the migrations and seeders, optionally adds sample products, and creates the admin account.</li></ol><p>After installation, <code>/install</code> redirects to the storefront.</p><p><a id="🐳-docker-installation"></a></p><h2 id="run-with-docker" tabindex="-1">Run with Docker <a class="header-anchor" href="#run-with-docker" aria-label="Permalink to &quot;Run with Docker&quot;">​</a></h2><h3 id="production-image" tabindex="-1">Production Image <a class="header-anchor" href="#production-image" aria-label="Permalink to &quot;Production Image&quot;">​</a></h3><p>The official images are built from <code>docker/production/</code> in the Bagisto repository. Each one bundles a web server, PHP 8.4, a database and an installed store with sample products:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 80:80</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webkul/bagisto:latest</span></span></code></pre></div><p>Open <code>http://localhost</code>, and the admin at <code>http://localhost/admin/login</code> with <code>admin@example.com</code> / <code>admin123</code>. Change that password on any store other people can reach.</p><table tabindex="0"><thead><tr><th>Web server</th><th>MySQL 8.0</th><th>MariaDB 10.11</th><th>PostgreSQL 16</th></tr></thead><tbody><tr><td>Nginx and PHP-FPM</td><td><code>latest</code>, <code>latest-nginx</code>, <code>latest-nginx-mysql</code></td><td><code>latest-nginx-mariadb</code></td><td><code>latest-nginx-postgres</code></td></tr><tr><td>Apache and mod_php</td><td><code>latest-apache</code>, <code>latest-apache-mysql</code></td><td><code>latest-apache-mariadb</code></td><td><code>latest-apache-postgres</code></td></tr><tr><td>OpenLiteSpeed and lsphp</td><td><code>latest-litespeed</code>, <code>latest-litespeed-mysql</code></td><td><code>latest-litespeed-mariadb</code></td><td><code>latest-litespeed-postgres</code></td></tr></tbody></table><p>To pin a release, replace <code>latest</code> with its version, for example <code>webkul/bagisto:2.5.0-nginx-postgres</code>. To use another host port, set <code>APP_URL</code> to match:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 8080:80</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> APP_URL=http://localhost:8080</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webkul/bagisto:latest</span></span></code></pre></div><p>The container reads <code>APP_URL</code>, <code>APP_KEY</code>, <code>APP_TIMEZONE</code>, <code>APP_LOCALE</code>, <code>APP_CURRENCY</code>, <code>APP_ADMIN_URL</code> and the <code>DB_*</code> variables from the environment. Set <code>DB_HOST</code> to anything other than <code>127.0.0.1</code> or <code>localhost</code> to use your own database instead of the bundled one. Without volumes, the data is lost when the container is removed; <a href="https://github.com/bagisto/bagisto/blob/master/docker/production/README.md" target="_blank" rel="noreferrer"><code>docker/production/README.md</code></a> lists the volumes to mount and every variable.</p><h3 id="docker-compose-workspace" tabindex="-1">Docker Compose Workspace <a class="header-anchor" href="#docker-compose-workspace" aria-label="Permalink to &quot;Docker Compose Workspace&quot;">​</a></h3><p>The <a href="https://github.com/bagisto/bagisto-docker" target="_blank" rel="noreferrer">bagisto-docker</a> repository is a development workspace with a choice of Nginx, OpenLiteSpeed or Apache runtimes, MySQL, phpMyAdmin and Mailpit. Its setup script installs Bagisto 2.4.7 and its runtimes run PHP 8.3, so it can&#39;t run Bagisto 2.5 yet. For a containerized Bagisto 2.5, use the <a href="#production-image">production image</a> or <a href="#laravel-sail">Laravel Sail</a>.</p><p><a id="⛵-laravel-sail-installation"></a></p><h3 id="laravel-sail" tabindex="-1">Laravel Sail <a class="header-anchor" href="#laravel-sail" aria-label="Permalink to &quot;Laravel Sail&quot;">​</a></h3><p>Bagisto&#39;s <code>docker-compose.yml</code> is a <a href="https://laravel.com/docs/sail" target="_blank" rel="noreferrer">Laravel Sail</a> file. It runs the application on PHP 8.4 alongside Redis, Elasticsearch, Kibana and Mailpit, and lets you choose the database — MySQL 8.0, MariaDB 10.11 or PostgreSQL 16 — and, if you want one, the web server in front of it. Sail itself isn&#39;t installed with Bagisto.</p><ol><li><p>Install Sail. On a machine without PHP, run Composer in a container from the project directory:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">):$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -g</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pwd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">):/var/www/html&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/www/html</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    laravelsail/php84-composer:latest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> require</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> laravel/sail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ignore-platform-reqs</span></span></code></pre></div><p>With PHP and Composer installed, run <code>composer require laravel/sail --dev</code> instead.</p></li><li><p>Point <code>.env</code> at the Sail services. <code>DB_HOST</code> is the database container&#39;s service name, and <code>COMPOSE_PROFILES</code> decides which containers start:</p><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DB_CONNECTION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=mysql</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DB_HOST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=mysql</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DB_PORT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=3306</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DB_DATABASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=bagisto</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DB_USERNAME</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=sail</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DB_PASSWORD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=password</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">COMPOSE_PROFILES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${DB_CONNECTION}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">REDIS_HOST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=redis</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">MAIL_HOST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=mailpit</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">MAIL_PORT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=1025</span></span></code></pre></div><p><code>.env.example</code> already carries the <code>COMPOSE_PROFILES</code> line. <code>DB_USERNAME</code> can&#39;t be <code>root</code> — the MySQL and MariaDB images fail to initialise when asked to create a user by that name.</p></li><li><p>Build and start the containers, then install Bagisto:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vendor/bin/sail</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-cache</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vendor/bin/sail</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vendor/bin/sail</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto:install</span></span></code></pre></div></li></ol><p>The store is at <code>http://localhost</code>, Mailpit at <code>http://localhost:8025</code> and Kibana at <code>http://localhost:5601</code>. Stop the containers with <code>vendor/bin/sail down</code>.</p><h4 id="choosing-the-database" tabindex="-1">Choosing the Database <a class="header-anchor" href="#choosing-the-database" aria-label="Permalink to &quot;Choosing the Database&quot;">​</a></h4><p>Each database is a Docker Compose profile named after its <code>DB_CONNECTION</code> value, so <code>COMPOSE_PROFILES=\${DB_CONNECTION}</code> starts the one the store is configured for and leaves the others out:</p><table tabindex="0"><thead><tr><th><code>DB_CONNECTION</code></th><th>Database</th><th><code>DB_HOST</code></th><th><code>DB_PORT</code></th></tr></thead><tbody><tr><td><code>mysql</code></td><td>MySQL 8.0</td><td><code>mysql</code></td><td><code>3306</code></td></tr><tr><td><code>mariadb</code></td><td>MariaDB 10.11</td><td><code>mariadb</code></td><td><code>3306</code></td></tr><tr><td><code>pgsql</code></td><td>PostgreSQL 16</td><td><code>pgsql</code></td><td><code>5432</code></td></tr></tbody></table><p>Only the container follows <code>DB_CONNECTION</code>. Every connection in <code>config/database.php</code> reads the same <code>DB_HOST</code> and <code>DB_PORT</code>, so set both from the table when you switch, and remove the <code>DB_PORT</code> line rather than emptying it — an empty value is an empty string, not the connection&#39;s default. Each database keeps its own volume, so switching points Bagisto at an empty server: run <code>vendor/bin/sail artisan bagisto:install</code> again.</p><p><code>COMPOSE_PROFILES</code> is Docker Compose&#39;s own variable, not Laravel&#39;s, and it belongs in <code>.env</code> because every command needs it, not only <code>up</code> — <code>sail down</code> without it stops the database container but leaves it behind. To try another database for a single command, prefix it instead, as in <code>COMPOSE_PROFILES=pgsql vendor/bin/sail up -d</code>. <code>sail up --profile pgsql</code> doesn&#39;t work: Compose accepts <code>--profile</code> only before the subcommand, and Sail appends arguments after it.</p><h4 id="running-behind-a-web-server" tabindex="-1">Running Behind a Web Server <a class="header-anchor" href="#running-behind-a-web-server" aria-label="Permalink to &quot;Running Behind a Web Server&quot;">​</a></h4><p>The application container serves the store with <code>artisan serve</code>. Add <code>nginx</code>, <code>apache</code> or <code>litespeed</code> to <code>COMPOSE_PROFILES</code> to put one of them in front of it:</p><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">COMPOSE_PROFILES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=pgsql,nginx</span></span></code></pre></div><p>The server listens on <code>http://localhost:8080</code>, which <code>FORWARD_WEB_PORT</code> changes, while port 80 still reaches <code>artisan serve</code> directly. It serves <code>public/</code> from the project and passes everything else to the application container, so static assets, rewrite rules, cache and security headers behave as they do in production — the configurations in <code>docker/local/</code> mirror the production images&#39; virtual hosts. The three are alternatives, since they share the same port.</p><p><a id="📱-mobile-app-installation"></a></p><h2 id="mobile-app" tabindex="-1">Mobile App <a class="header-anchor" href="#mobile-app" aria-label="Permalink to &quot;Mobile App&quot;">​</a></h2><p>Bagisto&#39;s open-source mobile app is a separate Flutter project. It talks to the store through the GraphQL API of the <a href="https://github.com/bagisto/bagisto-api" target="_blank" rel="noreferrer">bagisto-api</a> package, which you install in Bagisto first. The app&#39;s <a href="https://github.com/bagisto/opensource-ecommerce-mobile-app" target="_blank" rel="noreferrer">README</a> lists the Flutter, Dart, Android Studio and Xcode versions it needs, and where to set your store&#39;s endpoint.</p><p><a id="🛠️-common-troubleshooting"></a></p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><ul><li><p><strong>Pages or assets return 404.</strong> The web server&#39;s document root must be <code>public/</code>, not the project root.</p></li><li><p><strong>Images are broken.</strong> <code>APP_URL</code> must match the address you open, including the scheme and port. Also check that <code>public/storage</code> exists; <code>php artisan storage:link</code> creates it as a link to <code>storage/app/public</code>, where uploads are stored.</p></li><li><p><strong>Changes to <code>.env</code> have no effect.</strong> Clear the cached configuration with <code>php artisan optimize:clear</code>.</p></li><li><p><strong>Mixed content warnings behind HTTPS.</strong> <code>bootstrap/app.php</code> already trusts every proxy (<code>$middleware-&gt;trustProxies(at: &#39;*&#39;)</code>), so Laravel follows the <code>X-Forwarded-Proto</code> and <code>X-Forwarded-Host</code> headers your proxy or load balancer sends. Set <code>APP_URL</code> to the <code>https://</code> address, and make sure the proxy sends those headers.</p><p>If the proxy can&#39;t send them, force HTTPS URLs in the <code>boot()</code> method of <code>app/Providers/AppServiceProvider.php</code>, and import <code>Illuminate\\Support\\Facades\\URL</code> at the top of the file:</p><p><strong>File:</strong> <code>app/Providers/AppServiceProvider.php</code></p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Bootstrap any application services.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> boot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    ParallelTesting</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setUpTestDatabase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $database, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $token) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        Artisan</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;db:seed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isProduction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">forceScheme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;https&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></li></ul><h2 id="things-to-watch" tabindex="-1">Things to Watch <a class="header-anchor" href="#things-to-watch" aria-label="Permalink to &quot;Things to Watch&quot;">​</a></h2><p><a id="use-php-artisan-dev"></a></p><ul><li><p><strong><code>php artisan dev</code> needs setup first.</strong> Laravel 13&#39;s <code>dev</code> command starts three processes, listed by <code>php artisan dev:list</code>:</p><ul><li><code>server</code>: <code>php artisan octane:start --watch</code>, registered by <code>laravel/octane</code>, which Bagisto requires, in place of <code>php artisan serve</code>. It needs an Octane server installed and configured (RoadRunner, unless <code>OCTANE_SERVER</code> names another), and <code>--watch</code> needs Node.js and the <code>chokidar</code> package; see <a href="./../performance/configure-laravel-octane.html">Configure Laravel Octane</a>.</li><li><code>queue</code>: <code>php artisan queue:listen --tries=1 --timeout=0</code>.</li><li><code>vite</code>: the root <code>npm run dev</code>, which runs the Laravel skeleton&#39;s <code>vite.config.js</code> and doesn&#39;t build the admin or storefront assets. For those, run <code>npm run dev</code> inside <code>packages/Webkul/Shop</code> or <code>packages/Webkul/Admin</code>.</li></ul><p><code>dev</code> starts the processes through <code>@laravel/multiplex</code> (<code>concurrently</code> on Windows) with your package manager (<code>npx</code> for npm), so Node.js must be installed.</p></li></ul><h2 id="next-step" tabindex="-1">Next Step <a class="header-anchor" href="#next-step" aria-label="Permalink to &quot;Next Step&quot;">​</a></h2><p><a href="./../architecture/overview.html">Architecture Overview</a> maps where everything lives in your new project. When the store is ready to go live, follow <a href="./deployment.html">Deployment</a>.</p>`,70)]))}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
