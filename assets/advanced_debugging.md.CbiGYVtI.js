import{_ as s,o as a,a as t,a2 as i}from"./chunks/framework.DCcmcJcL.js";const u=JSON.parse('{"title":"Debugging Tips","description":"","frontmatter":{},"headers":[],"relativePath":"advanced/debugging.md","filePath":"advanced/debugging.md","lastUpdated":1789464754000}'),n={name:"advanced/debugging.md"};function o(h,e,d,l,r,p){return a(),t("div",null,e[0]||(e[0]=[i(`<h1 id="debugging-tips" tabindex="-1">Debugging Tips <a class="header-anchor" href="#debugging-tips" aria-label="Permalink to &quot;Debugging Tips&quot;">​</a></h1><p>Most problems in a Bagisto store come down to a handful of causes: an environment value that isn&#39;t what you think, a cache holding an old answer, a route, middleware or listener you didn&#39;t know about, or a query that runs far more often than it should. This page shows the tools that reveal each one, starting with what the application is actually running.</p><h2 id="start-with-what-is-running" tabindex="-1">Start with What Is Running <a class="header-anchor" href="#start-with-what-is-running" aria-label="Permalink to &quot;Start with What Is Running&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> about</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bagisto:version</span></span></code></pre></div><p><code>about</code> reports the environment, whether debug and maintenance mode are on, which of the configuration, event, route and view caches are built, the cache, database, log, mail, queue and session drivers, and whether <code>public/storage</code> is linked. A setting that seems to do nothing is often explained by one line of it: a cached configuration, the <code>sync</code> queue, or a mail driver other than <code>bagisto-dynamic-smtp</code>.</p><p>The admin&#39;s <strong>About</strong> configuration screen shows the same report with the Bagisto version, the database server, the search engine, the mail transport and storage added, each with a health indicator (<code>packages/Webkul/Admin/src/Helpers/SystemInformation.php</code>). Bagisto 2.4 has no such screen.</p><h2 id="the-debug-bar" tabindex="-1">The Debug Bar <a class="header-anchor" href="#the-debug-bar" aria-label="Permalink to &quot;The Debug Bar&quot;">​</a></h2><p>The bar comes from <code>barryvdh/laravel-debugbar</code>, a development dependency (version 4, whose classes are under <code>Fruitcake\\LaravelDebugbar</code>). <code>packages/Webkul/DebugBar</code> adds a <strong>Modules</strong> tab to it, and its <code>DebugBarServiceProvider</code> is already registered in <code>bootstrap/providers.php</code>.</p><h3 id="when-it-appears" tabindex="-1">When It Appears <a class="header-anchor" href="#when-it-appears" aria-label="Permalink to &quot;When It Appears&quot;">​</a></h3><p>The bar renders on a web request when all of these hold:</p><ul><li><code>APP_DEBUG=true</code>.</li><li><code>APP_ENV</code> is neither <code>production</code> nor <code>testing</code>.</li><li><code>DEBUGBAR_ENABLED</code> is unset or <code>true</code>. Set it to <code>false</code> to hide the bar and keep debug mode.</li><li>Development dependencies are installed. A <code>composer install --no-dev</code> build has no debug bar, and Bagisto&#39;s provider skips it.</li></ul><p>It never collects in the console, so an indexer or a queue worker doesn&#39;t build up its data.</p><div class="warning custom-block"><p class="custom-block-title">Leave <code>APP_DEBUG_ALLOWED_IPS</code> Empty</p><p><code>App\\Providers\\AppServiceProvider::register()</code> switches the bar on and off for the IPs in <code>APP_DEBUG_ALLOWED_IPS</code> through the <code>Barryvdh\\Debugbar\\Facades\\Debugbar</code> class, which version 4 doesn&#39;t ship. With any value set, every request and artisan command stops with <code>Class &quot;Barryvdh\\Debugbar\\Facades\\Debugbar&quot; not found</code>. Control the bar with <code>APP_DEBUG</code> and <code>DEBUGBAR_ENABLED</code> instead.</p></div><h3 id="the-modules-tab" tabindex="-1">The Modules Tab <a class="header-anchor" href="#the-modules-tab" aria-label="Permalink to &quot;The Modules Tab&quot;">​</a></h3><p><code>Webkul\\DebugBar\\DataCollector\\ModuleCollector</code> groups the request&#39;s work by Concord module, one entry per <code>Webkul\\&lt;Package&gt;</code>:</p><ul><li><strong>Models</strong>: each Eloquent model class retrieved, with the number of instances loaded.</li><li><strong>Views</strong>: each Blade view rendered from the package, or from a theme override under <code>resources/themes/&lt;package&gt;/</code>, <code>resources/admin-themes/&lt;package&gt;/</code> or <code>resources/vendor/views/&lt;package&gt;/</code>.</li><li><strong>Queries</strong>: each query that reads from one of the module&#39;s tables, with its bindings filled in and its duration.</li></ul><p>A page that loads hundreds of one model, or repeats a query for every product in a listing, shows up as a large count under one module.</p><h3 id="the-other-tabs" tabindex="-1">The Other Tabs <a class="header-anchor" href="#the-other-tabs" aria-label="Permalink to &quot;The Other Tabs&quot;">​</a></h3><p>The standard collectors for queries, views, models, cache, mail, jobs and HTTP client calls are on by default. Events, the current route, the session and the configuration are off; turn one on with its variable, such as <code>DEBUGBAR_COLLECTORS_EVENTS=true</code>.</p><h2 id="logs" tabindex="-1">Logs <a class="header-anchor" href="#logs" aria-label="Permalink to &quot;Logs&quot;">​</a></h2><p>Logging goes through Laravel&#39;s <code>stack</code> channel, which the default <code>.env</code> points at the <code>single</code> channel, one file at <code>storage/logs/laravel.log</code>:</p><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LOG_CHANNEL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=stack</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LOG_STACK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=single</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LOG_LEVEL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=debug</span></span></code></pre></div><p>Set <code>LOG_STACK=daily</code> to start a new file each day; <code>LOG_DAILY_DAYS</code> sets how many are kept, 14 by default. The exception behind a <code>500</code> is written there with its stack trace:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> storage/logs/laravel.log</span></span></code></pre></div><p>Write your own entries with the <code>Log</code> facade, passing details as context rather than building them into the message:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Illuminate\\Support\\Facades\\Log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Log</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">warning</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Shipping rate request failed.&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;order_id&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $order</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;status&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $response</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span></code></pre></div><h2 id="routes-and-middleware" tabindex="-1">Routes and Middleware <a class="header-anchor" href="#routes-and-middleware" aria-label="Permalink to &quot;Routes and Middleware&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route:list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --path=admin/catalog/products</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route:list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name=shop.checkout</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route:list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --path=admin/catalog/products/edit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span></span></code></pre></div><p>With <code>-v</code>, each route lists its middleware in the order it runs:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET|HEAD  admin/catalog/products/edit/{id} admin.catalog.products.edit › Webkul\\Admin\\Http\\Controllers\\Catalog\\ProductController@edit</span></span>
<span class="line"><span>          ⇂ web</span></span>
<span class="line"><span>          ⇂ Webkul\\Core\\Http\\Middleware\\PreventRequestsDuringMaintenance</span></span>
<span class="line"><span>          ⇂ Webkul\\User\\Http\\Middleware\\Bouncer</span></span>
<span class="line"><span>          ⇂ Webkul\\Core\\Http\\Middleware\\NoCacheMiddleware</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Package</th><th>Route files</th><th>Middleware</th></tr></thead><tbody><tr><td>Admin</td><td><code>packages/Webkul/Admin/src/Routes/web.php</code>, which loads <code>auth-routes.php</code> and, under the <code>APP_ADMIN_URL</code> prefix, <code>catalog-routes.php</code>, <code>sales-routes.php</code> and the other <code>*-routes.php</code> files beside it</td><td><code>web</code>, the maintenance check, then <code>admin</code> (<code>Webkul\\User\\Http\\Middleware\\Bouncer</code>) for everything but the sign-in routes</td></tr><tr><td>Shop</td><td><code>packages/Webkul/Shop/src/Routes/web.php</code>, which loads <code>webmcp-routes.php</code>, <code>store-front-routes.php</code>, <code>customer-routes.php</code> and <code>checkout-routes.php</code>, and <code>api.php</code></td><td><code>web</code>, the <code>shop</code> group, then the maintenance check</td></tr></tbody></table><p>The <code>shop</code> group runs three middleware from <code>Webkul\\Shop\\Http\\Middleware</code> in this order:</p><ol><li><code>Theme</code> sets the channel&#39;s theme when that theme is registered in <code>config/themes.php</code>, and <code>themes.shop-default</code> otherwise.</li><li><code>Locale</code> uses the <code>locale</code> query parameter when the channel has that locale, then the locale in the session, then the channel&#39;s default locale.</li><li><code>Currency</code> does the same with the <code>currency</code> parameter, the session and the channel&#39;s base currency.</li></ol><p>A storefront in the wrong theme, language or currency is almost always one of these three reading a channel that isn&#39;t set up the way you expect. An admin route that answers <code>401</code> for a restricted role usually has no ACL entry; see <a href="./common-pitfalls.html#admin-routes-need-an-acl-entry">Common Pitfalls</a>.</p><h2 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h2><p>Bagisto&#39;s events are dot-delimited strings, so <code>event:list</code> finds the listeners for one by name:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> event:list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --event=catalog.product.update.after</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>catalog.product.update.after ...............................................</span></span>
<span class="line"><span>⇂ Webkul\\Omnibus\\Listeners\\ProductPriceChange</span></span>
<span class="line"><span>⇂ Webkul\\CatalogRule\\Listeners\\Product@afterUpdate</span></span>
<span class="line"><span>⇂ Webkul\\FPC\\Listeners\\Product@afterUpdate</span></span>
<span class="line"><span>⇂ Webkul\\Product\\Listeners\\Product@afterUpdate</span></span>
<span class="line"><span>⇂ Webkul\\Shop\\Listeners\\CatalogCache@flush</span></span></code></pre></div><p>The list shows what a save sets off, and whether your own listener is registered at all. To see which events a request fires, and in what order, turn on the debug bar&#39;s events collector with <code>DEBUGBAR_COLLECTORS_EVENTS=true</code>. Where the core dispatches its events is covered in <a href="./event-listeners.html">Event Listeners</a>.</p><h2 id="queries" tabindex="-1">Queries <a class="header-anchor" href="#queries" aria-label="Permalink to &quot;Queries&quot;">​</a></h2><p>The debug bar covers the queries of a web request. For code you run by hand, record them in Tinker (<code>php artisan tinker</code>):</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Illuminate\\Support\\Facades\\DB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">use</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Webkul\\Product\\Repositories\\ProductRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DB</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">enableQueryLog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ProductRepository</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findBySlug</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;a-product-url-key&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DB</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getQueryLog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><h2 id="configuration-values" tabindex="-1">Configuration Values <a class="header-anchor" href="#configuration-values" aria-label="Permalink to &quot;Configuration Values&quot;">​</a></h2><p><code>config:show</code> prints a file-based value:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config:show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.admin_url</span></span></code></pre></div><p>Settings saved in the admin are rows in <code>core_config</code>, read with <code>core()-&gt;getConfigData($field, $channelCode, $localeCode)</code>. Without the channel and locale arguments it reads the current channel and locale, so a value saved only for another channel reads as the field&#39;s default from <code>system.php</code>, or <code>null</code> when the field has none:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tinker</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --execute=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;dump(core()-&gt;getConfigData(&#39;search_engines.general.products.storefront_mode&#39;))&quot;</span></span></code></pre></div><p>After editing <code>.env</code> or a file in <code>config/</code> on a store where <code>php artisan config:cache</code> has run, nothing changes until <code>php artisan optimize:clear</code>. <code>about</code> shows whether the configuration is cached.</p><h2 id="views" tabindex="-1">Views <a class="header-anchor" href="#views" aria-label="Permalink to &quot;Views&quot;">​</a></h2><p>To find which Blade file renders part of a page, turn on the <a href="./../tools/blade-tracer.html">Blade Tracer</a> with <code>&#39;tracer&#39; =&gt; true</code> in <code>config/view.php</code>; it labels each rendered view with its path. The debug bar&#39;s Views tab lists every view the request rendered, and the Modules tab groups them by package. The overlay appears on storefront pages; on admin pages, read the <code>data-blade-path</code> attributes in the browser&#39;s inspector.</p><h2 id="queues-and-scheduled-work" tabindex="-1">Queues and Scheduled Work <a class="header-anchor" href="#queues-and-scheduled-work" aria-label="Permalink to &quot;Queues and Scheduled Work&quot;">​</a></h2><p>The default <code>.env</code> sets <code>QUEUE_CONNECTION=sync</code>, so a queued job runs inside the request or command that dispatched it, and its exception surfaces there and in the log. With a real queue driver, a failed job is stored in the <code>failed_jobs</code> table:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> queue:failed</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> queue:retry</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> all</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> queue:work</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --once</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> schedule:list</span></span></code></pre></div><p><code>queue:work --once</code> processes one job in the foreground, which is the easiest way to watch a job run. <code>schedule:list</code> shows what the scheduler runs and when. Running workers and the scheduler on a server is covered in <a href="./queue-jobs-scheduling.html">Queues, Jobs and Scheduling</a>.</p><h2 id="caches" tabindex="-1">Caches <a class="header-anchor" href="#caches" aria-label="Permalink to &quot;Caches&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Symptom</th><th>Clear with</th></tr></thead><tbody><tr><td>Edits to <code>.env</code>, <code>config/</code>, routes or events are ignored</td><td><code>php artisan optimize:clear</code></td></tr><tr><td>A guest sees an old storefront page that a signed-in customer doesn&#39;t</td><td><code>php artisan responsecache:clear</code>; the full page cache is only served to guests</td></tr></tbody></table><p>What each cache holds and what invalidates it is on <a href="./cache-strategy.html">Cache Strategy</a>.</p><h2 id="environment-checks" tabindex="-1">Environment Checks <a class="header-anchor" href="#environment-checks" aria-label="Permalink to &quot;Environment Checks&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Symptom</th><th>Check</th></tr></thead><tbody><tr><td>Broken images or asset URLs</td><td><code>APP_URL</code> matches the URL you browse, and <code>about</code> shows <code>public/storage</code> as linked (<code>php artisan storage:link</code>)</td></tr><tr><td>A <code>500</code> with no detail</td><td><code>storage/logs/laravel.log</code>, and that <code>storage/</code> and <code>bootstrap/cache/</code> are writable</td></tr><tr><td>The admin URL returns <code>404</code></td><td><code>APP_ADMIN_URL</code> in <code>.env</code>, then <code>php artisan optimize:clear</code></td></tr><tr><td>The storefront shows the maintenance page</td><td>Maintenance mode in <code>about</code>, and the channel&#39;s <code>is_maintenance_on</code>; see <a href="./common-pitfalls.html#maintenance-mode-is-one-switch-plus-a-flag-per-channel">Common Pitfalls</a></td></tr><tr><td>Styles or scripts missing after a change</td><td><code>npm run build</code> in <code>packages/Webkul/Admin</code> or <code>packages/Webkul/Shop</code></td></tr><tr><td>Emails ignore the admin&#39;s SMTP settings</td><td><code>MAIL_MAILER=bagisto-dynamic-smtp</code>; see <a href="./common-pitfalls.html#smtp-settings-from-the-admin-are-ignored">Common Pitfalls</a></td></tr></tbody></table><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./common-pitfalls.html">Common Pitfalls</a>: the problems that come up most often, and their fixes.</li><li><a href="./queue-jobs-scheduling.html">Queues, Jobs and Scheduling</a>: running workers and the scheduler.</li><li><a href="./event-listeners.html">Event Listeners</a>: where core dispatches its events.</li></ul>`,61)]))}const k=s(n,[["render",o]]);export{u as __pageData,k as default};
