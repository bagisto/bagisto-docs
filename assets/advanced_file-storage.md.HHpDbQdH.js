import{_ as s,o as a,a as i,a2 as t}from"./chunks/framework.DCcmcJcL.js";const k=JSON.parse('{"title":"File Storage","description":"","frontmatter":{},"headers":[],"relativePath":"advanced/file-storage.md","filePath":"advanced/file-storage.md","lastUpdated":1789411704000}'),o={name:"advanced/file-storage.md"};function n(d,e,l,r,h,c){return a(),i("div",null,e[0]||(e[0]=[t(`<h1 id="file-storage" tabindex="-1">File Storage <a class="header-anchor" href="#file-storage" aria-label="Permalink to &quot;File Storage&quot;">​</a></h1><p>Product images, category banners, theme section media, uploaded search images and every other file Bagisto writes go to Laravel&#39;s <strong>default filesystem disk</strong>. Out of the box that is the local <code>public</code> disk, which <code>php artisan storage:link</code> exposes at <code>public/storage</code>. On Bagisto 2.5 the disk can also be Amazon S3 or Cloudflare R2, chosen from the admin, and this page explains how that switch works and what it does not do.</p><div class="info custom-block"><p class="custom-block-title">Availability</p><p>The <code>r2</code> disk, the <code>league/flysystem-aws-s3-v3</code> dependency and the <strong>Configure → File Management</strong> section belong to Bagisto 2.5. Bagisto 2.4 ships the <code>s3</code> disk definition in <code>config/filesystems.php</code> but no admin switch and no S3 adapter; there, install <code>league/flysystem-aws-s3-v3</code> yourself and set <code>FILESYSTEM_DISK=s3</code> in <code>.env</code>.</p></div><h2 id="disks" tabindex="-1">Disks <a class="header-anchor" href="#disks" aria-label="Permalink to &quot;Disks&quot;">​</a></h2><p><strong>File:</strong> <code>config/filesystems.php</code></p><table tabindex="0"><thead><tr><th>Disk</th><th>Driver</th><th>Used for</th></tr></thead><tbody><tr><td><code>public</code></td><td>local, <code>storage/app/public</code></td><td>Default disk: catalog and theme media, customer uploads</td></tr><tr><td><code>private</code></td><td>local, <code>storage/app/private</code></td><td>Import files, validation fragments, downloaded import images, error reports. Never served</td></tr><tr><td><code>local</code></td><td>local, <code>storage/app</code></td><td>Laravel default, unused by core</td></tr><tr><td><code>s3</code></td><td>S3</td><td>Amazon S3, from <code>AWS_*</code> env keys</td></tr><tr><td><code>r2</code></td><td>S3</td><td>Cloudflare R2, from <code>R2_*</code> env keys, path-style endpoint on by default</td></tr></tbody></table><p>The default is <code>env(&#39;FILESYSTEM_DISK&#39;, &#39;public&#39;)</code>.</p><p>Two things stay on the local <code>private</code> disk whatever the default: the Data Transfer import pipeline (it reads files by path, which a remote adapter cannot provide) and the CSV, XLS, XLSX and XML sources. Sitemaps are written to the <code>public</code> disk explicitly. Everything else, including product images, theme section images and the CMS editor&#39;s uploads, uses the default disk and therefore moves with it.</p><h2 id="choosing-the-disk-from-the-admin" tabindex="-1">Choosing the disk from the admin <a class="header-anchor" href="#choosing-the-disk-from-the-admin" aria-label="Permalink to &quot;Choosing the disk from the admin&quot;">​</a></h2><p>The <strong>File Management</strong> configuration group has three sections:</p><table tabindex="0"><thead><tr><th>Section</th><th>Keys</th></tr></thead><tbody><tr><td><code>file_management.general.settings</code></td><td><code>default_driver</code>: <code>public</code>, <code>s3</code> or <code>r2</code></td></tr><tr><td><code>file_management.amazon_s3.settings</code></td><td><code>key</code>, <code>secret</code>, <code>region</code>, <code>bucket</code>, <code>url</code>, <code>endpoint</code>, <code>use_path_style_endpoint</code></td></tr><tr><td><code>file_management.cloudflare_r2.settings</code></td><td><code>account_id</code>, <code>key</code>, <code>secret</code>, <code>bucket</code>, <code>url</code></td></tr></tbody></table><p>R2 asks for no region or endpoint: the endpoint is built from the account id as <code>https://{account_id}.r2.cloudflarestorage.com</code>, the region is forced to <code>auto</code> and path-style addressing is forced on.</p><p><code>Webkul\\Core\\Filesystem\\StorageConfigurator::configure()</code> runs from <code>CoreServiceProvider::boot()</code>, before anything resolves storage. It reads <code>core_config</code> directly (the channel-aware reader is not available that early), copies every filled-in setting onto <code>filesystems.disks.{s3|r2}.*</code>, and sets <code>filesystems.default</code> to the chosen driver. A setting left empty is passed over, so <code>.env</code> stays in charge of anything the admin has not filled in, and a driver whose adapter class is not installed is refused. <code>Webkul\\Core\\Enums\\SupportedFilesystemEnum</code> holds the three values and the adapter check.</p><div class="warning custom-block"><p class="custom-block-title">Existing files do not move</p><p>Switching the default disk changes where files are written from now on. Files already uploaded stay on the storage they were saved to, and their paths are stored without a disk name, so a product image saved locally is looked up on S3 after the switch and not found. Copy <code>storage/app/public</code> to the bucket before switching, or switch before uploading anything.</p></div><h2 id="urls-on-a-remote-disk" tabindex="-1">URLs on a remote disk <a class="header-anchor" href="#urls-on-a-remote-disk" aria-label="Permalink to &quot;URLs on a remote disk&quot;">​</a></h2><p>Two pieces of core were changed so that a remote disk behaves like a local one:</p><ul><li><strong>Image cache.</strong> <code>image_urls()</code> always returns <code>/cache/{template}/{path}</code> URLs, and <code>Webkul\\ImageCache\\Http\\Controllers\\ImageCacheController</code> reads the file through <code>Storage::get()</code> on the default disk before falling back to the local paths in <code>config/imagecache.php</code>. So product, category and swatch images are resized whichever disk holds them. On Bagisto 2.4 a non-local disk short-circuits to <code>Storage::url($path)</code> and serves the original file unresized.</li><li><strong>Theme media.</strong> <code>bagisto_theme_storage()</code> (<code>Webkul\\Theme\\ThemeStorage</code>) resolves section images at render time: a local disk yields a site-relative URL, a remote disk yields <code>Storage::url($path)</code>. Paths in <code>theme_section_translations</code> are stored bare, without host or <code>storage/</code> prefix, so a domain change or a disk change does not break them. Two migrations on Bagisto 2.5 rewrite older rows into this form.</li></ul><h2 id="environment-keys" tabindex="-1">Environment keys <a class="header-anchor" href="#environment-keys" aria-label="Permalink to &quot;Environment keys&quot;">​</a></h2><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FILESYSTEM_DISK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=public</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_ACCESS_KEY_ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_SECRET_ACCESS_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_DEFAULT_REGION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=us-east-1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_BUCKET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AWS_USE_PATH_STYLE_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=false</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_ACCESS_KEY_ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_SECRET_ACCESS_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_DEFAULT_REGION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=auto</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_BUCKET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">R2_USE_PATH_STYLE_ENDPOINT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=true</span></span></code></pre></div><p>Admin settings win over these when filled in. Use the env keys for a deployment where credentials must not live in the database, and leave the admin fields empty.</p><h2 id="writing-files-from-a-package" tabindex="-1">Writing files from a package <a class="header-anchor" href="#writing-files-from-a-package" aria-label="Permalink to &quot;Writing files from a package&quot;">​</a></h2><p>Use the <code>Storage</code> facade with no disk name for anything the store owner may want on object storage, and <code>Storage::disk(&#39;private&#39;)</code> for working files that must be read by path:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Storage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;rma/&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hashName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), $file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$url </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Storage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($path);</span></span></code></pre></div><p>Sanitize SVG uploads with the <code>Webkul\\Core\\Traits\\Sanitizer</code> trait (<code>sanitizeSVG($path, $mimeType)</code>), which strips remote references; core does this for locale logos, search images and review attachments.</p>`,24)]))}const g=s(o,[["render",n]]);export{k as __pageData,g as default};
